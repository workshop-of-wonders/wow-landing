# Changelog — Workshop of Wonders landing

This file is the working log for this project. **Claude: read this file at the start of any session on this repo, and append a new entry every time you finish a piece of work** (after commit+push), instead of relying on conversation memory. Keep entries short and factual — what changed and why, not a transcript of the conversation.

## How to use this file
- Add new entries at the **top** of the "Log" section (most recent first), one entry per meaningful change/commit.
- Each entry: date, one-line summary, commit hash if available.
- Keep the "Pending / open items" section current — remove items once resolved, add new ones as they come up.
- Keep the "Key decisions & conventions" section updated if a new durable pattern/convention is established (e.g. a breakpoint value, a naming rule).

---

## Project links

- **Repo**: https://github.com/workshop-of-wonders/wow-landing (branch `main`, push directly — no PR flow used so far)
- **Live site**: https://wow-landing-beta.vercel.app/ (auto-deploys from `main` via Vercel)
- **Vercel dashboard**: not confirmed — the Vercel MCP integration connected in this environment (team `somosefectowow-1205's projects`) does **not** list this project, so it's deployed under a different Vercel account/login than the one connected here. To check build logs or domain settings, log into vercel.com with whichever account owns `wow-landing-beta` directly (not through this session's tools).
- **Design assets**: `design-system/` folder in the repo (icons, logo, client logos, hero photos, font). Real Behance case-study images still need to be added here (see Pending below).
- **Behance (reference for case studies)**: https://www.behance.net/mjtamayol

## Pending / open items

- **Scope note (2026-08-21)**: after the 2026-08-20 site refocus, `index.html`'s work-gallery only has the 10 projects with a real web/SEO/marketing component (see that day's Log entries) — items below that name a project no longer on the home (Prepapp, Seed Capital, Orbit, etc.) apply to `portafolio.html` only, which still has all 22 original case studies untouched.
- **Real photos still needed for 3 brands (portafolio.html only)**: Prepapp, MJ Studio, and Seed Capital are still colored gradient cards (name + tagline, no photo, no lightbox — `.work-item-color`). AM Studios, Tata's Photos, and Geco. got real photos 2026-08-11 (see log) and were converted back to real `<button data-lightbox>` items; do the same for the rest once photos exist. (Moonking and Acústica Eafit were also in this state — Moonking still is; Acústica Eafit's card was removed 2026-08-14 per client request, back to logo-only in the marquee.)
- **`tiny.png` client logo identity unconfirmed**: `design-system/clients/tiny.png` (teal cursive mark) visually resembles the Tin-T! logo but wasn't confirmed against a source — left its caption/title as generic "Tiny" rather than guessing. Confirm with the client and either rename to Tin-T! or correct it.
- **Bary, Bilac, Tiny, Amlogo have only placeholder copy** (`data-desc="Desarrollo de Marca"`, no real project description): logo-only in the `.work-brands-track` marquee now (not clickable — see log), client explicitly chose to keep them marquee-only rather than give them portfolio cards. If real project copy/photos ever come in, they could get a proper `work-item` card like Clínica del Cerebro etc.
- **Lead form backend — frontend hardened 2026-08-20, still not connected**: the "Cuéntanos tu proyecto" modal now has real client-side validation, a honeypot + time-trap spam filter, and a proper enviando/éxito/error UI flow (see Log). A ready-to-deploy `api/contact.js` Vercel serverless function scaffold exists (Resend-based email send, server-side validation) but is **not deployed and not called from the frontend** — see "Lead form backend still not wired" below for exactly what's missing.
- **Client logo sizing**: logos are transparent + tight-cropped and rendered at a consistent height, but some may still look slightly different in visual weight due to each source file's original design (not fixable purely with CSS).
- **Phase 2 (copy/positioning/conversion) — mostly resolved 2026-08-19 (see Log)**: `data-work` for Ajá Waffles/Prepapp/Seed Capital rewritten (no longer template phrasing) and synced to `portafolio.html`; Moosy/Epika Store taglines fixed (no more asterisk on a functional word). Academy Lab now has a placement: a compact mention row at the end of `#servicios` linking to the project-form modal (not a 4th Lab, not its own section).
- **Phase 3 (portfolio copy voice) — resolved 2026-08-20 (see Log)**: audited and rewrote the remaining 9 template-y/duplicate `data-work` entries (Orbit, Indeleble, AM Studios, Tata's Photos, Pretty Pets, Geco., La Marquessa, Ser Niños) plus Carlos Ravelo's tagline (asterisk moved off the functional job-title word), synced across `index.html`/`portafolio.html`.
- **Social proof still needs a real number**: a `NECESITAMOS DATO REAL` placeholder sits above the client-logos marquee in `index.html` (`.work-brands-context`) — replace with a real stat (e.g. "+XX marcas") once the client confirms one. Do not invent a number.
- **`/admin` panel + CRM built 2026-08-20, needs manual Vercel setup before it works** (see Log entry below for full detail): the 3 contact forms now call `/api/contact` for real, and `api/contact.js` persists every submission to a `leads` Postgres table (email via Resend is now best-effort — a lead is never lost even if the email fails/isn't configured). None of this works yet in production until someone with Vercel dashboard access does the one-time setup: (1) create a Postgres database and a Blob store on the `wow-landing-beta` project (Storage tab); (2) run `scripts/schema.sql` against that database; (3) set env vars `ADMIN_USERNAME`, `ADMIN_PASSWORD_HASH` (generate with `node scripts/hash-password.mjs "contraseña"`), `ADMIN_JWT_SECRET` (any long random string), `GITHUB_TOKEN` (fine-grained PAT, `contents:write` on this repo only — used by the "Publicar" button to commit regenerated HTML), `GITHUB_OWNER=workshop-of-wonders`, `GITHUB_REPO=wow-landing`; (4) confirm `RESEND_API_KEY`/`CONTACT_TO_EMAIL` are set (may already exist from a prior session, unconfirmed); (5) run `node scripts/migrate-seed.mjs` once, pointed at that Postgres instance (`POSTGRES_URL` env var), to load the 21 existing portfolio projects into the database and add `data-slug` anchors to `index.html`/`portafolio.html` — review that diff before committing, it should only add attributes, not change visible content.
- **New convention**: once the admin panel above is live, portfolio project text/photos should be edited through `/admin` → "Publicar", not by hand in `index.html`/`portafolio.html` — hand-editing a `work-item` block will get silently overwritten the next time someone publishes that project's slug from the admin panel.
- **Resolved 2026-08-20 (Phase 5, see Log)**: `ProfessionalService` `knowsAbout` JSON-LD updated to the current 23-service inventory, and the one genuine `translations.en` orphaned key removed. Both modals (`#lightbox`, `#projectModal`) now have `role="dialog"`/`aria-modal`/`aria-labelledby`, and the FAQ accordions on `index.html`/`servicios.html` now toggle `aria-expanded`/`aria-controls` correctly.
- ~~**Section-merge recommendation, not implemented**: Proceso (`#metodologia`) and Diferencial (`#diferencial`)...~~ — **moot as of 2026-08-20**: `#diferencial` ("Continuamos") was removed from `index.html` entirely per direct user instruction, so there's nothing left to merge Proceso with.

## Key decisions & conventions

- **Breakpoints**: `900px` is the main mobile/tablet cutoff used across the site (hero, labs accordion, nav). `720px` and `560px` are used for a few finer adjustments. When adding a new mobile override, check whether JS logic (e.g. `window.innerWidth` gates) needs to match the same breakpoint as the CSS — this caused a bug once (see 2026-08-08, labs accordion).
- **`--page-x`**: `clamp(24px, 6vw, 96px)` — the horizontal section padding variable. Use this instead of a fixed px value for any new section's left/right padding so it scales with viewport width.
- **Falling icons (contact section)**: Matter.js physics playground. Known-sensitive settings: `restitution: 0.9` + default solver iterations (bouncy, piles up — client's preferred feel, don't "fix" the piling/clumping without asking, it's intentional). Drag release is hardened across `mouseup/touchend/touchcancel/pointerup/blur`. If you ever call `Render.setPixelRatio` again, remember to also set `mouse.pixelRatio` to the same value (and re-set it on resize) — Matter's `Mouse` module doesn't pick this up automatically, and drag silently breaks on any scaled/HiDPI display without it.
- **`flex-basis` on axis-flip bug**: watch out — properties like `flex: 0 1 520px` written for a *row* layout become a fixed **height** if that flex container switches to `flex-direction: column` at a breakpoint (e.g. hero-content, hero-media, lightbox-desc all hit this). Always re-check/reset `flex-basis` inside mobile media queries after adding a fixed basis for desktop.
- **Hub diagram connector lines**: drawn dynamically in JS (`hubArrows` SVG) from real pill positions, not hardcoded paths — recalculates on resize/font-load.
- **Case-study modal**: dark bottom bar (name, category, description, tags) + an editorial image collage on top, built by `ProjectCollage` (see below). Triggered by `[data-lightbox]` on both work-items and client-logos.
- **`ProjectCollage`** (defined inline in `index.html`'s bottom `<script>`, styled in `styles.css` under `.project-collage`/`.pc-item`): a generic, reusable "automatic editorial collage" — give it any array of image URLs (or `{src, priority}` objects) and it measures each image's real aspect ratio, builds a fixed 2-row CSS grid (`grid-auto-flow: column dense`) that grows horizontally with `overflow-x: auto`, and sets each item's pixel width from its own ratio at the row height (`--pc-row-h`) — never `object-fit: cover`, never cropped/stretched. A handful of portrait or high-`priority` images get promoted to "tall" (`.pc-span2`, spans both rows) to vary the rhythm; the browser's dense packing fills the rest with no per-project logic or hardcoded positions. Any `[data-lightbox]` element can opt into a real, curated set via `data-images='["a.png", {"src":"b.png","priority":2}]'`; without it, the modal falls back to the project's single `data-img` plus 3 images seeded from the shared `hero-photos` pool (legacy behavior, unchanged). No project (Epika, Otto, or otherwise) has a bespoke layout — this replaces a dead/never-finished `lightboxImg1-4` implementation that referenced DOM elements no longer in the markup.
- **Project form modal**: triggered by any `[data-open-form]` element (nav "Hablemos", hero CTA, services CTA, contact pill, hub "W" click).

---

## Log

**2026-08-23** — Fixed the client-logo carousel (`.work-brands-marquee`, "Ellos creen en nosotros" scrolling logos) on `index.html` sitting glued directly against the bottom of the work-gallery portfolio cards with zero gap, per user report ("los logos quedan muy pegados a las cards del portafolio" → clarified as "el carrusel de logos"). Root cause: the `.work-brands-label`/`.work-brands-context` wrapper that used to sit between the two and carried `margin-top: var(--space-6)` was removed from `index.html`'s markup at some point (no trace of when — CSS rules for both classes are still in `styles.css` but now unused/orphaned), so the marquee lost its only source of top spacing. The same overlap-only geometry audit logged just below this entry didn't catch it because a 0px gap isn't an *overlap* (nothing exceeded the >40%-shared-area threshold), just a spacing/crowding gap it wasn't checking for — worth remembering: any future "espacio"/"breathing room" style check needs an explicit min-gap pass between stacked sibling sections, not just a collision pass. Fix: added `margin-top: var(--space-6)` (48px) directly to `.work-brands-marquee` instead of resurrecting the removed label markup. Verified via local static server + JS geometry check: gap between `.work-gallery`'s bottom edge and `.work-brands-marquee`'s top edge is now 48px (was 0px). Committed and pushed to `main` (`b8674d7`).

**2026-08-23** — Full overlap/spacing audit across `index.html`, `servicios.html`, `portafolio.html` per user request ("revisa que en el sitio web de wow nada se sobreponga y todo respire"), triggered by the recent section-reorder history. Since screenshot capture wasn't available in this session's browser pane, verification was done via JS-driven geometry checks in a live local preview instead of visual screenshots: an automated collision detector walked every visible text/image/button node (`checkVisibility()`-filtered, so hidden/collapsed/zero-opacity elements are excluded) and flagged any pair of unrelated elements whose bounding rects overlapped by >40% of the smaller one's area, at 375/768/1440px, with every interactive/expandable state forced open first (mobile nav drawer, both Labs accordion panels, all FAQ answers, the work-gallery "door" reveal, and the portfolio lightbox modal). Also checked `document.documentElement.scrollWidth` vs `innerWidth` for horizontal overflow at each width/state, console errors, hero-vs-fixed-nav clearance, hub-diagram pill collisions (the JS-computed `hubArrows` SVG case), the falling-icons canvas/footer boundary, and the two documented `flex-basis` axis-flip past victims (`hero-media`/`hero-content` reset to `flex-basis: auto` correctly under the 900px column-flip; `lightbox-desc` has no fixed basis at all, confirmed `flex-basis: auto` at 375px with the lightbox open). **Result: no genuine overlap or crowding bugs found on any of the 3 pages at any of the 3 widths, in any expanded state.** The detector's only two flagged pairs were both confirmed as intentional, not bugs: (1) the large low-opacity `.outline-word` watermark text (e.g. "TRABAJO", "PROCESO") sitting behind section H2s — `color: transparent`, `-webkit-text-stroke: 1px`, `opacity: 0.3`, `z-index: 0`, explicitly hidden under 720px, by design a background decoration, not colliding content; (2) mobile-nav-open state flagging nav link text against page content underneath it — the drawer is `position: fixed; z-index: 55` with an opaque `rgba(20,4,34,0.92)` panel background, so the "content" underneath is fully occluded, not visually overlapping. No CSS or HTML changes were made — nothing to fix. This likely reflects the multiple prior 2026-08-20 QA/audit passes already logged below having caught the reorder-era regressions. No commit-worthy code change; this entry documents the audit itself per the log-every-pass convention.

**2026-08-20** — Removed the ES/EN language toggle from `index.html` entirely, per user request ("elimina el inglés") — confirmed with the user this meant a full code cleanup, not just hiding the button. Removed: the two `.lang-toggle` buttons (desktop + mobile nav), the whole i18n JS system (the ~256-line `translations.en` dictionary, `LANG_KEY`/`DYNAMIC_ATTRS`/`I18N_ATTR_NAMES` vars, the `setLanguage()` function, the init IIFE that read `localStorage`/attached the toggle click listeners, and the custom-cursor's manual `[data-i18n]` re-pass), every `data-i18n` boolean attribute and `data-desc-en`/`data-category-en`/`data-tagline-en`/`data-work-en`/`data-i18n-aria-label-en`/`data-i18n-placeholder-en` attribute across the page (removed via regex, not by hand — ~250+ attribute instances), the now-dead `translations`-guarded ternary in the contact form's submit handler (simplified to a plain `'Enviando…'` string), and the unused `.lang-toggle`/`.lang-toggle-mobile`/`.lang-toggle.is-en` rules in `styles.css`. Left every plain `data-desc`/`data-category`/`data-tagline`/`data-work`/`data-capabilities` attribute untouched — those are separate, still-active machinery for the hub-diagram popup, mega-menu popup, and project lightbox, unrelated to translation. `servicios.html` and `portafolio.html` never had the toggle, so nothing to remove there. Verified locally on `index.html` and `servicios.html`: no console errors, toggle button confirmed absent from the DOM, hub-pill popup text confirmed still working (reads the untouched `data-desc`). Committed and pushed to `main`.

**2026-08-20** — Adapted webcreativa.com.co's "Nuestra agencia" paragraph for `index.html`'s `#filosofia` section (the "about us" blurb, previously labeled "Qué es WOW") — user pasted the exact webcreativa text as the model to follow. Kept its sentence structure and rhythm ("nos mueve llevar el posicionamiento digital de [tu marca] al siguiente nivel, planteando soluciones basadas en el entendimiento estratégico y analítico...") but swapped in WOW's actual services (diseño web, SEO, pauta digital) instead of webcreativa's more branding-flavored phrasing, and addressed the reader directly ("tu negocio"/"tu marca") instead of the third-person "su negocio" webcreativa uses. Relabeled the eyebrow from "QUÉ ES WOW" to "NUESTRA AGENCIA" to match. Updated `translations.en` for both strings. Verified locally: no console errors, new eyebrow/intro text confirmed rendering correctly via JS. Committed and pushed to `main`.

**2026-08-20** — Rewrote on-page SEO copy on `index.html` and `servicios.html` to be more keyword-dense, per user request ("necesito textos seo oriented, guíate de los de webcreativa.com.co") — webcreativa's title/hero pattern leads directly with the target keyword phrase ("Diseño de página web en Medellín... Agencia de Marketing Digital") rather than brand-first, creative copy. Applied the same pattern (keyword-first, without inventing a city — WOW positions as remote across Colombia/LatAm, unlike webcreativa's Medellín focus): **`index.html`** — `<title>`/meta description/OG/Twitter changed from "Workshop of Wonders — Desarrollo web, SEO, SEM y analítica de datos" to "Agencia de Marketing Digital y Diseño Web | Workshop of Wonders" with a denser description; hero eyebrow changed from the brand name alone to "AGENCIA DE MARKETING DIGITAL Y DISEÑO WEB" (now translatable, added `data-i18n` + EN string); hero-sub rewritten to list every core service explicitly (added "diseño web" and "desarrollo de sitios y e-commerce" which weren't named before); `ProfessionalService` JSON-LD description and the JS `headEn` EN-toggle title/desc updated to match. **`servicios.html`** — same title/meta/OG/Twitter pattern ("Servicios de Diseño Web, SEO y Marketing Digital"), `Service` JSON-LD `serviceType` updated, hero eyebrow/H1/sub rewritten to lead with "Agencia de marketing digital" and list all current services including diseño gráfico. Verified locally: no console errors, ES↔EN toggle confirmed working correctly for the new eyebrow/title text on `index.html`. Committed and pushed to `main`.

**2026-08-20** — SEO audit + fix on `index.html` per user request ("lo necesito super seo"). Checked: title/description length (both within recommended ranges), single `<h1>`/clean `<h2>` hierarchy (confirmed intact after the section reorder), 100% image `alt` coverage (54/54), sitemap.xml/robots.txt (already correct, no changes needed), canonical/OG/Twitter tags (already correct). **Found and fixed one real gap**: the home had zero structured data beyond a flat `ProfessionalService.knowsAbout` array — no `hasOfferCatalog` (servicios.html has one) and no `FAQPage` schema despite a visible 5-question FAQ section on the page, meaning Google couldn't show rich service listings or FAQ snippets for the home specifically. Added both: a `hasOfferCatalog` mirroring servicios.html's 4 service categories (including the newly-added Diseño gráfico and Cursos digitales), and a `FAQPage` schema matching the on-page FAQ's 5 questions verbatim. Verified locally: no console errors, both new `<script type="application/ld+json">` blocks parse as valid JSON via JS (`ProfessionalService` + `FAQPage`). Committed and pushed to `main`. **Not done** (flagged, not attempted — needs real data/access this session doesn't have): keyword research, backlink strategy, Core Web Vitals/PageSpeed measurement, Google Search Console verification, local-SEO city targeting (WOW positions as 100% remote across Colombia/LatAm, not a single city like webcreativa's Medellín focus, so no city keywords were added).

**2026-08-20** — Added "Diseño gráfico" and "Cursos digitales" as services on `index.html` and `servicios.html`, per user instruction ("en wow yo hago lo mismo que webcreativa.com.co") — saved as project memory ([[wow_positioning_webcreativa]]) that WOW's positioning is meant to directly match webcreativa.com.co's, not just take loose inspiration from it. Compared webcreativa's 5 service categories (E-commerce, Soluciones web a la medida, Marketing digital, Cursos digitales, Diseño gráfico) against WOW's current list and found these two missing — flagged to the user (including the contradiction that a "Formación en IA" CTA banner covering similar ground had just been removed for having too many CTAs) before adding, per explicit confirmation to add both. Added "Diseño gráfico" to the Brand & Experience Lab group (nav mega-menu, hub diagram, Labs panel, `servicios.html`'s Experiencia digital section + JSON-LD) and "Cursos digitales" to the Insight Lab group everywhere except `servicios.html`, where it got its own new `#cursos` section/OfferCatalog (closer to webcreativa's structure, where it's a standalone category) — updated the `svc-jump` quick-nav, the `ProfessionalService` JSON-LD `knowsAbout` array, and both pages' "¿Qué servicios ofrece…" FAQ answer (now "tres frentes" instead of "dos"), with matching `translations.en` entries. Verified locally on both pages: no console errors, Labs/nav/servicios card lists confirmed via JS to include the two new services in the right groups. Committed and pushed to `main`.

**2026-08-20** — Removed two redundant CTAs from `index.html`'s `#servicios` hub diagram, per user feedback ("no necesito tantos cta") — the page already has "Hablemos de tu proyecto" in the nav (desktop + mobile), hero, and contact section. Removed: (1) the "Hablemos de tu proyecto" link that sat under the hub's center logo (`.hub-cta-link`), and (2) the "¿Tu equipo necesita subir el nivel? También ofrecemos Formación en IA…" banner below the hub diagram (`.academy-mention`) — cleaned up its now-orphaned `translations.en` entry too. Left the per-service buttons inside the Labs panels untouched (those act as clickable list items that prefill the contact form with a specific service, not a generic repeated CTA). Left the CSS rules (`.hub-cta-link`, `.academy-mention`) in `styles.css` in place, unused but harmless. Verified locally: no console errors, both elements confirmed absent from the DOM. Committed and pushed to `main`.

**2026-08-20** — Text pass over `index.html` after the webcreativa.com.co-inspired reorder: reviewed hero, servicios, labs, trabajo, and contacto copy and found them already direct/benefit-first enough to leave as-is, but the `#metodologia` (Proceso) section still had a leftover reference to the removed branding-strategy step — its step 2 was labeled "DEFINE · Core Lab" (Core Lab was cut from the site's scope weeks ago along with #diferencial, but this one label survived). Rewrote the section h2/intro to be more concrete ("Medimos, construimos y volvemos a medir." instead of the more abstract "De lo que vemos a lo que hacemos. Y de vuelta."), relabeled step 2 to "02 — PLAN" (dropped the dangling Core Lab reference, no lab name substituted since no lab covers that generic planning step), and rewrote its h3/p plus step 3's h3/p to describe building/launching a site or campaign instead of the old brand-identity framing — all with matching `translations.en` updates. Verified locally: no console errors, `.method-num` list confirmed as CRECE/PLAN/CREA/CRECE with no "Core Lab" text remaining. Committed and pushed to `main`.

**2026-08-20** — Reordered `index.html`'s section flow to match the structure of https://www.webcreativa.com.co/ (hero → services front-and-center → clients/trust → about-us blurb near the end), per user request. Old order: hero → filosofia (Qué es WOW) → labs → servicios (hub diagram) → metodologia → trabajo → faq → contacto. New order: **hero → servicios (hub diagram) → labs (service-category cards) → trabajo (client work, webcreativa's "Ellos creen en nosotros" equivalent) → filosofia (Qué es WOW, webcreativa's "Nuestra agencia" equivalent) → metodologia (Proceso) → faq → contacto**. Pure DOM reorder via a line-range PowerShell script (each `<section>` is a self-contained top-level block, `#lightbox` modal kept immediately after `#trabajo` since it belongs to that section's gallery) — no content, ids, or nav links changed, so every `#anchor` still resolves correctly, it just scrolls to a different position now. Verified locally: no console errors, `main > section` order confirmed via JS as hero/servicios/labs/trabajo/filosofia/metodologia/faq/contacto, hero+work-gallery render correctly in a screenshot. Committed and pushed to `main`.

**2026-08-20** — Rewrote the "Qué es WOW" (`#filosofia`) section on `index.html` — it was the last remaining spot still pitching "estrategia, marca, diseño, tecnología y marketing" (branding-strategy language left over from before the refocus). User referenced https://www.webcreativa.com.co/ for tone/structure inspiration (direct, benefit-first copy); kept the section's existing structure (3-toggle list) but rewrote the h2, intro, and all 3 toggle lines to talk about web dev + data-driven growth instead of brand strategy, with matching `translations.en` updates. Did not reorder page sections or restructure other parts of the home — scoped this pass to the one section that still had stale branding copy. Verified locally: no console errors, `#filosofia h2` renders the new copy correctly. Committed and pushed to `main`.

**2026-08-20** — Follow-up fixes after the index-web promotion, per user instruction ("la página de servicios debe tener los mismos servicios del home" + "elimina la sección de continuamos"). (1) **Fixed an incomplete edit from the promotion itself**: the previous commit's plan to rewrite the 6 kept-but-mixed work-gallery projects' branding mentions never actually applied (the Edit call that would have done it had silently failed, and the miss wasn't caught before committing) — `data-capabilities`/`data-work`/`data-work-en` for Lámparas Milán, Ajá Waffles, Pretty Pets, Geco., Epika Store, and Clínica del Cerebro still said things like "Refresh y rebranding…", "Identidad y diseño de marca", "DEFINE - Core Lab". Rewrote all 6 now to drop every branding-service mention, keeping only the web/SEO/marketing work actually described (a first attempted fix via a line-index PowerShell helper had a marker-matching bug that left old and new text concatenated together on 4 lines — caught by grep and corrected with a second pass). (2) **`servicios.html` rescoped to match the new home**: removed the `#brand-strategy` (Estrategia de marca) and `#brand-creative` (Identidad y diseño de marca) sections entirely — page now only has Experiencia digital / Crecimiento y marketing digital / Analítica y datos, exactly the 3 categories on the home. Updated title/meta/OG/Twitter tags, the `Service` JSON-LD's `serviceType` and `hasOfferCatalog` (dropped the two branding `OfferCatalog` entries), the `FAQPage` JSON-LD and the on-page FAQ (rewrote the "qué servicios" / "cuánto cuesta" / "cuánto tiempo toma" / "puedo contratar solo un servicio" answers to talk about web+SEO/marketing instead of branding/rebranding/identity), the hero H1/subhead, and the `svc-jump` quick-nav. (3) **Removed the "Continuamos" / `#diferencial` section** from `index.html` (the DEFINE→CREA→CRECE→APRENDE loop diagram) per direct instruction — it framed the old 4-Lab branding+web+growth cycle, which no longer matches the site's scope; deleted the section and its 5 now-orphaned `translations.en` entries (kept the unrelated footer quote that shared the file region). No section linked to `#diferencial`, so nothing else needed updating. Verified all three pages locally via static server: no console errors, `index.html`'s work-gallery capabilities/work text confirmed branding-free, `#diferencial` confirmed absent from the DOM. Committed and pushed to `main`.

**2026-08-20** — Site refocus: `index-web.html` promoted to be the official home (`index.html`), old branding+web+marketing `index.html` deleted from the working tree (full history preserved at git tag `backup-pre-refoco`, pushed to `origin`, pointing at commit `0baec26`). Before the swap: (1) renamed the "Desarrollo Web" labs panel/nav-dropdown-category/hub-pill back to `Brand & Experience Lab`/`CREA` (matching the original home's naming) while keeping only the pruned service list (Diseño web, UX/UI Design, E-commerce) — removed a now-orphaned `"DESARROLLO": "DEVELOP"` translation entry. (2) Removed the 12 work-gallery/portfolio-card projects that were pure branding with no web/SEO/marketing component (Orbit, Arlo, Tin-T!, Moosy, Indeleble, Prepapp, Seed Capital, AM Studios, Tata's Photos, Moonking, Carlos Ravelo, Walkmate), keeping the 10 that have a real web/marketing component (Lámparas Milán, Ajá Waffles, Pretty Pets, Geco., Epika Store, Centro del Sueño y Ronquido, Clínica del Cerebro, Caminos de la Vida, La Marquessa, Ser Niños) and rewrote the 6 mixed ones' `data-work`/`data-capabilities` to drop branding-service mentions (naming, identidad visual, rebranding, propuesta de valor), keeping only the web/SEO/marketing part actually done. Also swapped the hero photo carousel's pure-branding cards (Tin-T!, Moosy, Arlo, Indeleble, AM Studios, Orbit, Tata's Photos) for repeats of the 6 kept projects that have a `-vertical.webp` hero photo available, and fixed `Lámparas Milán`'s hero-card blurb to drop "y Diseño de Marca". `portafolio.html` was explicitly left untouched (all 22 original case studies, including the removed-from-home ones, still live there) per user instruction. Fixed the promoted page's canonical URL / `og:url` / JSON-LD `url` from `/index-web.html` to `/`. Verified locally via static server: no console errors, work-gallery shows exactly the 10 kept projects, hero carousel shows only the 6 kept titles. Committed and pushed to `main`.

**2026-08-20** — Added `404.html`: custom not-found page with the same header/nav (logo, links back to `index.html` anchors, mobile burger menu with its own minimal inline JS, no dependency on `index.html`'s inline script) and a purple-background hero showing the brand mascot (`design-system/404/mascota-404.png`, sourced from the user's `Desktop/WoW/1x/Recurso 2.png`) plus a "Volver al inicio" CTA. Vercel serves any `404.html` at the project root as the static 404 fallback automatically — no `vercel.json` routing needed. Verified locally via a static server at 800px width (header, mascot, and copy all render correctly). Committed and pushed to `main`.

**2026-08-20** — Created `index-web.html` (renamed from `desarrollo-web.html`): a full duplicate of `index.html`, re-scoped to a services page covering only desarrollo web (web design/UX-UI/e-commerce), SEO, SEM/pauta digital, and analítica de datos — `index.html` itself untouched (still the branding+web+marketing home page). Removed the "Core Lab"/DEFINE branding-strategy category everywhere (nav mega-menu, `#labs` panel, hub diagram) and pruned "Brand & Experience Lab"/CREA down to only its web-development services (Diseño web, UX/UI Design, E-commerce), relabeling that column/panel/pill "Desarrollo Web"; the Insight Lab/SEO-SEM-analytics category was kept in full. Nav mega-menu now has 2 columns instead of 3 (Desarrollo Web / SEO, SEM & Analítica); hub diagram's right-side category pills reduced from DEFINE/CREA/CRECE to DESARROLLO/CRECE. Rewrote head metadata (title, meta description, OG/Twitter tags, canonical → `/index-web.html`) and the `ProfessionalService` JSON-LD `knowsAbout` array to the web-dev/SEO/SEM/analytics service set. Rewrote hero H1/subheadline, the `labs-intro` paragraph, the Brand panel's h3/paragraph, and the home FAQ's first 3 Q&As to match the new scope, adding matching `translations.en` entries (plus updating the JS `headEn` title/desc used by the ES↔EN toggle) for every changed string so the language toggle still works correctly on the new page. Portfolio/work-gallery, testimonials-adjacent marquee, footer, and all image/asset paths were left completely unchanged, including branding-only portfolio captions (e.g. "Identidad de marca" on individual case studies), which are real past work and out of scope for this rescoping. Verified `index.html` line count unchanged (2403) and confirmed via grep that no leftover Core-Lab/branding-service terms (Propuesta de valor, Auditoría de marca, Arquetipo, Identidad de marca, Rebranding, Packaging, Copywriting, Diseño editorial, Presentaciones, Misión/visión/valores) remain in `index-web.html`'s nav/labs/hub sections — only in portfolio captions, which is expected. Committed and pushed to `main`.

**2026-08-20** — Built `/admin`: private panel + CRM dashboard. First `package.json`/npm dependencies in this repo (`@vercel/postgres`, `@vercel/blob`, `bcryptjs`, `jsonwebtoken`, `@octokit/rest`). Added: Postgres schema (`scripts/schema.sql`) for `projects`, `project_images`, `leads`, `login_attempts`; a set of `/api/admin/*` serverless functions (login/logout with bcrypt + JWT cookie + IP rate-limit, CRUD for projects, image add/reorder/delete via Vercel Blob client-upload, leads list/status/notes); a plain-JS static SPA at `/admin` (no build step — served like any other static file) covering login, a project list/editor with drag-to-reorder image grid, and a leads dashboard with status filters (new/contacted/won/lost) and a notes field. `api/contact.js` now inserts every submission into `leads` before attempting the Resend email, so a lead is never lost even if email sending fails or isn't configured yet; the 3 forms' `fetch('/api/contact', …)` calls (previously commented out, see prior entries) are now live. Publishing an edited project writes the regenerated `<button data-lightbox>` block back into `index.html`/`portafolio.html` via the GitHub Contents API and commits to `main` (`api/admin/_publish.js`) — chosen over converting the public pages to a framework or fetching data client-side, to keep them exactly as static/fast as today; `data-slug` is the stable anchor the regenerator matches on (added by `scripts/migrate-seed.mjs`, not present yet — needs that one-time migration run, see Pending). None of this is live in production until the manual Vercel setup (DB, Blob, env vars, migration run) happens — see Pending.

**2026-08-20** — Phase 5 technical QA pass across `index.html`, `portafolio.html`, `servicios.html` (no commit/push, per instruction), covering 7 client-authorized technical items not requiring new business input. (1) **Labs/Servicios/Proceso/Diferencial redundancy re-check**: re-read `#labs` vs hub `#servicios` and `#metodologia` vs `#diferencial` phrase by phrase — found the Phase-4 rewrite of the hub's DEFINE/CREA/CRECE tooltips into catalog-style lists already holds (Labs keeps the why/how paragraphs, hub lists what's included), and Proceso/Diferencial's shared "medimos/aprendemos" language is inherent to describing the same CRECE→APRENDE loop from two different angles (process mechanism vs. post-launch promise), not literal duplication — no changes made, matches the decision already logged 2026-08-20 earlier in this file. (2) **GEO/CRO copy**: reviewed `data-desc` for both across nav dropdown, hub pills, and Labs — already framed as business outcomes ("optimizamos tu marca para aparecer en las respuestas de ChatGPT, Google AI…", "maximizamos las conversiones de tu sitio…"), not technical jargon — left untouched. (3) **Portfolio copy audit**: re-read all 21 `data-work` entries in `portafolio.html` against `index.html` — found Carlos Ravelo's `data-work` still generic ("Diseño de identidad de marca para una marca personal enfocada en consultoría y estrategia empresarial.", basically restating `data-desc`) and rewrote it in both files to name the actual angle (personal brand for a consultant, identity meant to convey the same rigor as his advisory work) — no invented facts, same category/capabilities. **Bug found in the same edit**: Carlos Ravelo's `data-tagline`/`data-tagline-en` attributes were duplicated on the `index.html` button (an old, wrongly-emphasized version *and* the Phase-3 "fixed" version both present) — per the HTML spec, the browser keeps the *first* duplicate attribute and ignores the rest, so the Phase-3 tagline fix from 2026-08-20 earlier today had never actually rendered. Removed the stale first pair, kept the corrected one; verified in-browser afterward that the live DOM shows the fixed tagline. (4) **`translations.en` orphan cleanup + JSON-LD**: wrote a small PowerShell check comparing every `translations.en` key against the rendered HTML body — found and removed one true orphan, the "La marca sigue creciendo… No desaparecemos después de lanzar" key (its Filosofía toggle item was deleted in the 2026-08-19 Phase-2 pass but the translation entry was left behind); the footer-quote key flagged by a naive text search is a false positive (its HTML source uses `&ldquo;`/`&rdquo;` entities that the browser decodes to curly quotes before `data-i18n` reads `innerHTML` — same documented gotcha from 2026-08-17/19). Updated the `ProfessionalService` JSON-LD `knowsAbout` array, which still listed only 9 old service names, to a representative spread of the current 23-service inventory (added Arquetipo y personalidad de marca, Rebranding, E-commerce, GEO, CRO) — no invented services. (5) **SEO technical check**: confirmed `<title>`/meta description/OG/Twitter tags are already distinct and relevant per page (home, portafolio, servicios all have their own copy, no duplication); confirmed `sitemap.xml` lists all 3 HTML pages with correct URLs and `robots.txt` allows everything with a sitemap reference — no changes needed. Added `loading="lazy"` to the one recurring below-the-fold image missing it across all 3 pages: the footer WOW logo (`footer-logo-img`), previously eager-loaded on every page. No missing/generic `alt` text found. (6) **Accessibility QA — found and fixed real gaps**: neither modal (`#lightbox` case-study modal on `index.html`/`portafolio.html`, `#projectModal` project-request form on all 3 pages) had `role="dialog"`, `aria-modal="true"`, or `aria-labelledby` — the existing focus-trap (Tab cycling + Escape-close + focus-return, built 2026-08-19) worked correctly but the modals were invisible as modals to screen readers. Added `role="dialog" aria-modal="true" aria-labelledby="…"` to both, pointing at each modal's existing/newly-added title `id` (`lightboxTitle` already existed; added `projectModalTitle` to the form modal's `<h3>`). Separately, the FAQ accordion (`.svc-faq-q`/`.svc-faq-a`, on `index.html`'s 5-question home FAQ and `servicios.html`'s 6-question FAQ) had no `aria-expanded`/`aria-controls` at all, neither static nor JS-managed — added unique `id`s to every answer panel, `aria-controls` on each question button, and updated both pages' accordion click handlers to toggle `aria-expanded="true"/"false"` on open/close. Verified in-browser via JS execution: FAQ toggle now flips `aria-expanded` correctly and collapses siblings, both modals report `role="dialog"` in the live DOM, project-modal open→Escape→focus-return confirmed still working end-to-end with a visible trigger element, ES↔EN round-trip (including `aria-pressed` on the lang toggle) confirmed correct. (7) **Section-merge evaluation (not implemented)**: considered merging `#metodologia` (Proceso) and `#diferencial` (Diferencial) since both touch the same measure/learn idea, but concluded a merge would need to either fold Diferencial's post-launch promise into Proceso's 4-step diagram (losing its standalone "we don't stop after launch" framing, which reads as a distinct commercial promise, not just a process step) or cut Proceso's own explanatory depth — a structural/copy decision, not a mechanical one, so left both sections as-is and flagging this as a recommendation for the client to decide in a future phase rather than executing it here. No other low-risk merge candidate identified. Verified in-browser (local static server, not `file://`) at 375/768/1440px on all 3 pages: no console errors, no horizontal overflow (`scrollWidth` matches `innerWidth` at 375px on `index.html`/`servicios.html`; 1425 vs 1440 at desktop width on `index.html`, i.e. no overflow), FAQ accordion + `aria-expanded` working on `index.html` (5 items) and `servicios.html` (6 items), project-modal `role="dialog"`/focus-trap/Escape-close/focus-return confirmed on `portafolio.html` and `index.html`, lightbox `role="dialog"` confirmed on `portafolio.html`, ES↔EN toggle round-trip confirmed on `index.html`. **Not done, left as-is per instruction/scope**: `api/contact.js` activation, analytics, ICP, testimonials/results, Academy Lab positioning, visual identity/typography/color/animation — none touched.

**2026-08-20** — Contact-form real-destination groundwork across `index.html`, `portafolio.html`, `servicios.html`, `styles.css` (no commit/push, per instruction). Confirmed first (git log/grep across all 3 HTML files) that no backend/integration existed at all, not even a partial `fetch()` with a placeholder URL — only the `projectRequest` payload object and a `// TODO: conectar...` comment from a prior phase. Implemented everything possible without inventing credentials: (1) **Reinforced client-side validation**: added `novalidate` to the form (native `:invalid` styling is inconsistent across browsers) and a JS `validate()` that trims whitespace, checks a stricter email regex, and adds a `.has-error` class + inline `.project-field-error` message under the email/name fields specifically (the two `required` fields) — non-blocking fields (company/need/budget/details) are untouched. (2) **Spam protection, 100% frontend, no external service**: a honeypot field (`name="website"`, visually hidden via clip-path/absolute positioning — not `display:none`, which some bots specifically check for — `tabindex="-1"` and `aria-hidden="true"` so it's unreachable by keyboard/screen readers) plus a time-trap (timestamp set on modal open, submits under 2s are treated as bot activity). Both cases silently show the success state without processing anything, so a bot never learns the trap tripped. Documented in-code as a first-line filter only, not a replacement for server-side validation. (3) **Real 3-state UI flow (enviando/éxito/error)**: submit now disables the button and swaps its label to "Enviando…"/"Sending…" (translated via the existing `translations.en` dict on `index.html`) before showing the result. Chose the **honest-copy option** over faking a "sent" state: since no backend is deployed yet, the success message was reworded from "¡Gracias! Te responderemos..." to "¡Listo! Tu solicitud quedó registrada. Te responderemos en un máximo de 1 día hábil." (EN synced) — accurate about what's actually happening (the lead is captured client-side, ready to send once wired) without falsely implying the team already received it. The full sending→success UI plumbing (disable button, swap label, call an endpoint, re-enable on error) is implemented and exactly where a real `fetch('/api/contact', …)` call would go — that block is written out in full but commented, immediately after a `// Real integration point once api/contact.js is deployed and configured:` marker in each file's submit handler; today it's a 450ms `setTimeout` standing in for the network round-trip. A field-validation error (red border + inline message) is the only "error" state exercised today, since there's no live network call yet to fail. (4) **`api/contact.js` scaffold**: a complete, deployable Vercel serverless function (auto-detected from `/api`, no `vercel.json`/build step needed) that accepts POST, re-validates `email`/`name` server-side (never trusts the frontend alone), re-checks the honeypot field, and sends the lead via Resend's HTTP API using `process.env.RESEND_API_KEY`/`CONTACT_TO_EMAIL`/`CONTACT_FROM_EMAIL` — no hardcoded credentials, no assumption the env vars exist (returns a clear `500 not_configured` if they're missing, logged server-side). This is real, functional infrastructure code, not a fake integration — but it is **not deployed and not called from the frontend yet**, deliberately, since activating a `fetch()` against an endpoint with no API key configured in Vercel would fail in production. (5) **CSS**: added `.project-field-hp` (honeypot, clip-path technique), `.project-field.has-error`/`.project-field-error`, `.project-form-error`, and `.project-submit[disabled]` styles, following the existing form's spacing/color system — no layout/grid changes to the existing fields. Applied identically to `portafolio.html` and `servicios.html` (both ES-only, no i18n toggle — new copy is Spanish-only there, matching the rest of those pages), keeping every existing field name/id/structure unchanged. Verified in-browser via local static server at 375/768/1440px on all 3 pages: form fills and opens correctly, honeypot invisible and not part of the visible layout, field-level error state correctly triggers on invalid email + empty name (tested by bypassing the time-trap), sending→success flow confirmed (button disables, label changes to "Enviando…"/"Sending…", then shows the honest success copy), honeypot + time-trap both correctly short-circuit to a silent fake-success without touching `projectRequest`, ES↔EN round-trip correct for every new string on `index.html`, focus-trap/Escape-close unaffected, no new console errors on any page/width. **Ready for commit**: yes — no commit/push made per instruction; client still needs to decide on an email provider and confirm the destination inbox before `api/contact.js` can go live (see updated Pending section).

**2026-08-20** — Phase 4 conversion/redundancy pass on `index.html` only (no commit/push, per instruction; client asked for a minimal final report this round). (1) **Labs vs. Servicios hub redundancy**: the hub diagram's 3 DEFINE/CREA/CRECE category pills (`.hub-col-right`) had `data-desc`/`data-desc-en` tooltips that closely paraphrased Labs' own panel paragraphs (e.g. hub DEFINE said "definimos quién eres, qué representas y hacia dónde vas" vs. Labs Core Lab's "Construimos la estrategia que define quién eres..." — same conceptual explanation, reworded). Rewrote all 3 tooltips to a catalog-style service list instead (e.g. DEFINE now reads "Core Lab: propuesta de valor, auditoría de marca, arquetipo y personalidad, misión, visión y valores.") — Servicios now points at *what's included*, Labs keeps the *why/how* explanation; no pill removed, all 23 service pills untouched. (2) **Proceso vs. Diferencial**: compared both sections phrase by phrase — found genuine topical overlap (both touch "measure/learn" since that's inherent to the CRECE→APRENDE loop) but no literal or near-literal duplicate sentence, so left both untouched per instruction (only trim on clear duplication). (3) **Prueba social**: `.work-brands-context` above the client-logo marquee read the generic "Marcas y proyectos entregados en distintas industrias." — replaced with "Marcas y proyectos entregados en salud, moda, retail, tecnología y otras industrias.", using only real, already-visible category variety (Healthcare/Neurology, Healthcare/Sleep, Healthcare/Senior Care, Fashion/Sportswear, Fashion/Retail, Fashion/Accessories, Retail categories across multiple projects, Digital Marketing, etc. — all present in existing `data-category` attributes), no invented number/testimonial. Synced the `translations.en` EN string. (4) **FAQ redundancy**: found a genuine duplicate — Q1 ("¿Qué servicios ofrece Workshop of Wonders?") already answers "Puedes contratar un servicio puntual o un proyecto integral de principio a fin," and Q6 ("¿Puedo contratar solo un servicio, como SEO o rediseño de marca?") asked the same question with different examples and answered with the same core fact. Removed Q6 (both the visible accordion item and its now-orphaned `translations.en` entries) — home FAQ is now 5 questions, still accordion-functional. (5) **CTA flow**: audited the order/spacing of every "Hablemos de tu proyecto" instance (nav, hero, hub-cta-link in Servicios, contact-pill at the end) — hero's CTA is expected/standard, the next appearance is in Servicios after Filosofía+Labs have already given context, and the final one closes the page at Contacto; no CTA found appearing too early or in an excessive unbroken run, so no change made. (6) **Form microcopy**: reviewed all `project-form` labels/placeholders/help text — no ambiguity or awkward tone found, left untouched per instruction (don't touch if already fine). Explicitly out of scope and untouched: visual identity, animations, component/markup/CSS structure, `portafolio.html` content (only re-verified, no edits), SEO tags/JSON-LD, Academy Lab mention. Verified in-browser via local static server (not `file://`): no console errors on `index.html`, `portafolio.html`, or `servicios.html`; no horizontal overflow at 375/768/1440px; nav height still 88px at 768px (no regression of the earlier nav-overlap fix); FAQ accordion opens/closes (now 5 items); ES↔EN round-trip correct on hero and the new social-proof line; lightbox opens correctly (`Lámparas Milán` case study). **Ready for commit**: yes — no commit/push made per instruction.

**2026-08-20** — Phase 3 portfolio copy-voice pass across `index.html` and `portafolio.html` (no commit/push, per instruction). Audited the ~15 `data-work` entries not already rewritten in Phase 2/P0 (Ajá Waffles, Prepapp, Seed Capital, Moosy, Walkmate, Centro del Sueño y Ronquido, Caminos de la Vida were already narrative/specific from earlier passes and left untouched) and found 9 that still read as interchangeable template copy or had a weak tagline. Rewrote, in both files, keeping every fact already present in `data-desc`/`data-category`/`data-capabilities` and inventing nothing: **Orbit** (was "Creación de la marca desde cero, incluyendo naming, logo, colores y diseño de marca." → now names the actual deliverables and the market it positions the brand into); **Indeleble** (generic "Desarrollo del naming y diseño de identidad de marca." → leads with the "point of departure was the name" framing, distinct construction from the other naming-led rewrites); **AM Studios** (generic "Desarrollo de naming, logo, colores e identidad visual para la agencia." → leads with the "look as solid as its results" angle); **Tata's Photos** (generic "Desarrollo de naming, propuesta de valor, colores e identidad de marca." → leads with "personal brand for a photographer"); **Pretty Pets** (generic "Diseño de identidad de marca, estrategia de marketing digital y pauta digital." → ties the identity work explicitly to carrying it into sales channels); **Geco.** (generic "Diseño de identidad de marca y desarrollo de estrategia de marketing digital." → ties the digital strategy to reaching a "conscious audience," drawn from its Eco-friendly category); **La Marquessa** (generic "Estrategia de marketing digital, pauta digital y optimización del sitio web." → active-voice "we optimized / we designed" framing); **Ser Niños** (generic "Optimización del sitio web y estrategia de pauta digital para impulsar las ventas." → reframed around the single sales-focused goal, different sentence shape from La Marquessa's rewrite so the two no longer read as swapped-noun twins). Three of these (Indeleble, AM Studios, Tata's Photos) were a **semantic-duplicate cluster** — different wording, same underlying "naming + logo/colors + identity" template — resolved by giving each a distinct opening move (a reveal framing, an outcome framing, a subject framing) rather than just swapping synonyms. Also fixed **Carlos Ravelo**'s tagline, which had the asterisk landing on the job-title noun ("*Consultor* de estrategia de negocios." / "Business strategy *consultant*.") — moved the emphasis onto the substantive phrase instead ("Consultor de *estrategia de negocios*." / "*Business strategy* consultant."), same fix pattern as Moosy/Epika Store's tagline corrections in the prior phase, no new words introduced. Left untouched, per scope: Milan, Arlo, Tin-T, Epika Store, Clínica del Cerebro (already specific enough — mention concrete details like Arlo's excluded logo/mascot scope or Tin-T's named characters, not template phrasing), Moonking (different markup, no `data-work` attribute), and all of Labs/Servicios/FAQ/Academy Lab/the project form. Verified in-browser via a temporary local static server (not `file://`, so relative asset paths and CSS load correctly): Orbit's lightbox confirmed rendering the new `data-work` text correctly; no console errors on `index.html`, `portafolio.html`, or `servicios.html` at 375/768/1440px; ran `grep -c` on `data-work="` / `data-tagline="` in both files afterward to confirm no attribute quoting broke during the edits (21/18 matches in `index.html`, consistent with the pre-edit count).

**2026-08-20** — P0 QA fix pass across `index.html`, `portafolio.html`, `styles.css` (no commit/push, per instruction), fixing 4 issues a post-Phase-2 creative review flagged as blocking. (1) **Placeholder visible in production**: `.work-brands-context` (social-proof line above the client-logo marquee) had a literal `[NECESITAMOS DATO REAL]` text node visible in the rendered UI (plus a matching `translations.en` dictionary entry with the same placeholder baked into both the ES key and EN value). Removed the visible placeholder entirely — the line now reads "Marcas y proyectos entregados en distintas industrias." (EN: "Brands and projects delivered across different industries.") with no fabricated number; the dev marker survives only as an HTML comment (`<!-- NECESITAMOS DATO REAL: cifra real de marcas/proyectos cuando el cliente la confirme -->`) directly above it, never as visible text. (2) **SEO/hero mismatch**: `<title>`, `meta[name=description]`, `og:title`/`og:description`, `twitter:title`/`twitter:description`, the `ProfessionalService` JSON-LD `description` field, and the `headEn` JS object (which overwrites those same tags on EN toggle) were all still the pre-Phase-2 "Diseño, marca y marketing digital bajo un mismo techo..." line from before the hero copy changed. Aligned all of them to the current hero message: ES "Branding, sitio web y marketing digital, hechos por el mismo equipo que conoce tu marca de principio a fin." / title "Workshop of Wonders — Branding, sitio web y marketing digital, hechos por el mismo equipo"; EN "Branding, website, and digital marketing, built by the same team that knows your brand end to end." / title "Workshop of Wonders — Branding, Website & Digital Marketing, Built by the Same Team". Did not touch the JSON-LD's other fields (`knowsAbout`, `sameAs`, etc.) or run a full SEO audit — out of scope. (3) **Verbatim duplicate portfolio copy**: ran `grep -o 'data-work="[^"]*"'`/`data-work-en` across both `index.html` and `portafolio.html` to find every exact-duplicate description (not just the 2 examples flagged by QA, which turned out to already not match the current file — trusted the grep over the stale examples). Found 2 real duplicate pairs: Moosy/Walkmate both had `data-work="Diseño de identidad de marca."`, and Centro del Sueño y Ronquido/Caminos de la Vida both had `data-work="Diseño y desarrollo del sitio web, estrategia de marketing digital y pauta digital."`. Rewrote each pair to lead with what's specific to that client (Moosy → "Desarrollo de identidad visual para una tienda de moda con personalidad propia."; Walkmate → "Identidad de marca diseñada para una empresa de paseo de perros en Australia."; Centro del Sueño y Ronquido → "Sitio web, estrategia de marketing digital y pauta digital para un centro especializado en sueño en Medellín."; Caminos de la Vida → "Estrategia de marketing digital, pauta digital y desarrollo de sitio web para un hogar geriátrico."), varying sentence construction between each pair so they don't read as the same template with swapped nouns; synced the same rewritten `data-work` in `portafolio.html` (which has no `data-work-en` attribute at all, so only the ES string needed syncing there). Re-ran the same grep after editing — 0 duplicates left in either file. (4) **Nav overlapping hero text on scroll (desktop/tablet)**: diagnosed empirically in-browser rather than guessing — at 1440×900 the nav renders at its expected 88px height (matches `--nav-h`), but at 768px width the full desktop `.nav-links` (Servicios/Labs/Trabajo/Portafolio + lang toggle + CTA button) don't fit one row and wrap, so the nav's real rendered height becomes 108px while every hero/section offset in `styles.css` still assumes the fixed `--nav-h: 88px` — that 20px gap is what let `.hero-sub`/`.hero-sub-secondary` scroll in partially behind the opaque `.is-scrolled` nav background. Root cause was the mobile burger-nav breakpoint (`@media (max-width: 720px)`) being narrower than the breakpoint where the desktop mega-menu already turns off (`@media (max-width: 900px)`), leaving a 721–900px dead zone with no compact nav. Fix: moved the nav-collapse rules (`.nav-links`/`.nav-burger`/`.nav-mobile-menu` display toggles) out of the 720px block and into the existing 900px block, so the burger nav now activates at the same 900px boundary as the dropdown — verified nav height is exactly 88px at 375/768/1440px afterward, and the hero text now fully exits above the viewport before the nav ever gets near it. Left the 720px block's own rules untouched (`.hero`/`.services`/`.contact`/`.work`/`.labs`/`.hero-cta` padding) — no z-index changes, no change to the mega-menu's own hover/focus-within behavior. Verified in-browser (local static server, not `file://`) at 375/768/1440px: no `NECESITAMOS DATO REAL`/bracket placeholder anywhere in rendered text; nav no longer covers hero text at any scroll position; no new horizontal overflow; nav (Portafolio link included), lightbox open/Escape-close, and ES↔EN toggle (including the corrected `headEn` title/description) all confirmed working from the live DOM; no new console errors (the only console errors seen were pre-existing 404s for two missing Moosy lightbox extra images, unrelated to this change); JSON-LD/meta read back from the DOM match the live hero copy. **Ready for commit**: yes — all 4 P0s fixed and verified in-browser at the 3 required widths, no regressions found; commit/push still withheld per instruction.

**2026-08-19** — Phase 2 (content/positioning/conversion) pass across `index.html` and `portafolio.html`, implementing the client's final content brief on top of today's earlier Phase 1 cleanup. No visual identity/typography/color/animation changes beyond what each new section needed. Highlights: (1) Removed the leftover English H2 "We build brands, experiences and growth systems." in `#labs` — `.labs-intro`'s paragraph already carries that structural role, so no replacement H2 was added. (2) Linked `portafolio.html` from `index.html`: a "Ver el portafolio completo →" link under `#trabajo`'s H2, plus a "Portafolio" item in the desktop nav. (3) Rewrote `data-work` for the 3 flagged near-duplicate-template projects — Ajá Waffles (leads with the yuca-waffle product differentiator), Prepapp (leads with the character-universe angle), Seed Capital (leads with the podcast/media angle, not a product brand) — in both `index.html` and `portafolio.html`, no invented metrics, described as deliverables/work done rather than results. (4) Fixed Moosy's and Epika Store's taglines, which had an asterisk landing on a functional word ("*Tienda*", "*Tienda de productos*") — reworded to real ideas ("Moda con *carácter propio*", "Magia y estilo de *Disney* y *Universal*"), same fix applied to `portafolio.html`. (5) New hero subheadline, exact client copy: "Branding, sitio web y marketing digital, hechos por el mismo equipo que conoce tu marca de principio a fin." plus a new short secondary line ("Sin reexplicar tu proyecto cada vez que cambias de proveedor.") — H1 untouched. (6) New "No terminamos cuando lanzamos." differential section (`#diferencial`, between Proceso and Trabajo) with a DEFINE→CREA→CRECE→APRENDE→↻ cycle — a distinct, shorter conceptual diagram from Proceso's own 4-step circular mechanism, not a redesign of it; no "traditional agency" comparison anywhere in the copy, per client rule. Filosofía (`#filosofia`, now "Qué es WOW") and Proceso (`#metodologia`) were reviewed against the "merge without losing DEFINE/CREA/CRECE" instruction from today's brief — found already correctly split from earlier work today (Filosofía = human promise/integration, Proceso = the only section explaining the circular mechanism), so no further merge was needed; both anchors kept since neither is linked from the nav anyway. (7) Social proof: added a context line above the client-logos marquee with a `NECESITAMOS DATO REAL` placeholder (visible in the DOM, styled as a flagged/dashed marker, not published as a real claim) — no invented number. (8) Added an Academy Lab mention: a single compact row at the end of `#servicios` ("¿Tu equipo necesita subir el nivel? También ofrecemos Formación en IA...") linking to the project-form modal via `data-open-form` — not a 4th Lab, not its own scroll section. (9) Replaced the Walt Disney footer quote with original WOW copy: "Seguimos aquí después de lanzar — porque una marca que se siente real no termina en la entrega." (10) Elevated 2 tactical service descriptions in the nav-dropdown + hub diagram (both instances each) from task-level to outcome-level language: "Creación de posts gráficos para pauta" and "Análisis de métricas de sitios web." (11) Form backend: submit handler now builds a named `projectRequest` object (email/name/company/need/budget/details/lang/page) and carries a `// TODO: conectar aquí el backend real...` comment marking the exact future `fetch()` point — still submits nowhere, unchanged local thank-you-message behavior. (12) Added a 6-question FAQ section to the home page (`#faq`, reusing `servicios.html`'s `.svc-faq` CSS/JS pattern and its exact 6 questions verbatim, per client's explicit "reuse is fine") with its own accordion JS block. **Bug found and fixed along the way**: the new footer-quote translation-dictionary key was written using literal HTML entities (`&ldquo;`/`&rdquo;`) as its lookup key, but the i18n mechanism captures `el.innerHTML` *after* the browser has already decoded those entities into real Unicode curly-quote characters — so the key never matched and the EN toggle silently fell back to Spanish for that one string (same class of bug documented in this changelog from 2026-08-17's NBSP/`data-tagline` gotcha). Fixed by rewriting the dictionary key with the actual Unicode `"`/`"` characters. Verified in-browser: ES→EN→ES round-trip on every new string (hero-sub/secondary, diff-section, academy mention, FAQ, footer quote) reads correctly both directions; FAQ accordion opens/closes; project-form modal opens/fills/submits (local thank-you, no network calls) and Escape-closes with focus restored; lightbox opens/closes correctly; all `a[href^="#"]` anchors resolve; both `application/ld+json` blocks still parse; no console errors on `index.html` or `portafolio.html`; no horizontal overflow at 375/768/1440px, including the new `#diferencial` cycle diagram wrapping cleanly to 2 rows on mobile. **Not done, flagged in Pending above**: no real social-proof number, no lead-form backend integration, `ProfessionalService`/`headEn` JSON-LD copy audits and `translations.en` orphaned-key cleanup.

**2026-08-19** — Phase 2 (content/positioning) pass across index.html, portafolio.html, servicios.html, on top of the Phase 1 cleanup below. Verified via git diff first that several requested items (portfolio link to portafolio.html from #trabajo, the "We build brands..." English H2 residue, Aja Waffles/Prepapp/Seed Capital rewritten data-work, the FAQ section on the home page, and the projectRequest payload/TODO comment on index.html forms) were already present in the working tree from an earlier uncommitted pass -- did not redo them, only filled the gaps. (1) Hero: .hero-sub replaced with the client exact required copy ("Branding, sitio web y marketing digital, hechos por el mismo equipo que conoce tu marca de principio a fin."), plus a new short secondary line .hero-sub-secondary ("Sin reexplicar tu proyecto cada vez que cambias de proveedor."), both with EN translations and new CSS. H1 left untouched. (2) Filosofia/Proceso: removed Filosofia 4th toggle item ("La marca sigue creciendo... No desaparecemos despues de lanzar") since that promise now lives in its own section -- Filosofia remaining 3 items focus purely on the integration promise. Proceso (#metodologia) left completely untouched. Confirmed neither #filosofia nor #metodologia are linked from the nav, so no anchor-rename risk. (3) New "No terminamos cuando lanzamos." section (#diferencial, between Proceso and Trabajo): a short, non-comparative differential section (never mentions other agencies) showing the cycle DEFINE -> CREA -> CRECE -> APRENDE -> (loop back to) DEFINE as 5 pill nodes with arrows, seeded from the old Filosofia item 4 idea. New CSS block .diff-section/.diff-cycle/.diff-node/.diff-arrow. Full EN translations added. (4) Social-proof placeholder: added one line above the client-logo marquee -- [NECESITAMOS DATO REAL] marcas y proyectos entregados en distintas industrias -- styled with a visibly distinct dashed-border/amber placeholder chip so it reads as an obvious pending item, not a real claim; also left an HTML comment flagging it. No number invented. (5) Academy Lab mention: added one compact clickable row at the end of #servicios ("Tu equipo necesita subir el nivel? Tambien ofrecemos Formacion en IA, talleres y acompanamiento estrategico para equipos internos"), wired to the existing project-form modal. Not a 4th Lab, no new full section. (6) Portfolio near-duplicate copy: rewrote data-work/data-work-en for Aja Waffles, Prepapp, and Seed Capital (previously sharing the "[Desarrollo/Construccion] integral de la marca desde cero, incluyendo naming..." template) into CLIENTE/PROBLEMA/QUE HICIMOS structure leading with what is specific to each (Aja Waffles: cassava-waffle differentiator; Prepapp: character/universe-building for a young audience; Seed Capital: podcast/media angle) -- no invented metrics. Already synced between index.html and portafolio.html. (7) Moosy/Epika Store taglines: fixed the asterisk misplaced on a functional word -- Moosy Tienda -> "Moda con caracter propio."; Epika Store Tienda de productos -> "Magia y estilo de Disney y Universal." (8) Form backend: confirmed the projectRequest object + TODO comment (no real fetch) was already wired on index.html submit handler; added the identical pattern to portafolio.html and servicios.html submit handlers, which only had bare preventDefault()+thank-you toggle before. (9) FAQ: confirmed the 6-question FAQ accordion was already present on index.html before the final #contacto CTA, with working accordion JS and EN translations -- no changes needed. (10) Footer quote: confirmed the Walt Disney quote was already replaced on index.html; found portafolio.html and servicios.html still had the old quote (no EN toggle on those pages) and fixed both. Added the missing translations.en entry for the footer quote on index.html -- the key had to match the real rendered curly-quote characters, not the ampersand-entities used in the source markup, otherwise EN toggle silently fell back to Spanish; verified in-browser after the fix. (11) Tactical data-desc language: confirmed "Creacion de posts graficos para pauta" and "Analisis de metricas de sitios web" already had the elevated benefit-first copy across nav dropdown, hub pills, and Labs -- no changes needed. Verified in-browser (local static server): no console errors on any of the 3 pages; ES-EN-ES round-trip correct for all new/changed strings including the footer quote fix; all internal anchors resolve (15/15); 375/768/1440px -- no horizontal overflow on the new #diferencial section or the marquee placeholder line; project-form modal (fill, submit, thank-you, Escape-close) and lightbox (open, Escape-close) both still work with focus-trap intact; marquee still 13 visible / 13 aria-hidden duplicates. Nav also got a portafolio.html link added (desktop + mobile) alongside the existing #trabajo -> portfolio link.

**2026-08-19** — Phase 1 cleanup and bug-fix pass across `index.html`, `portafolio.html`, and `servicios.html` (no redesign, no copy/positioning rewrite — per explicit client scope). (1) **Marquee duplication**: `.work-brands-track`'s intentional doubled logo set (for the seamless CSS loop) had no `aria-hidden` on its second half, so screen readers announced all 13 client logos twice; added `aria-hidden="true"` and emptied `alt` on the duplicate half's 13 `<img>`s (kept real `alt` text on the first, visible half). (2) **Verbatim duplicate `data-work` copy**: La Marquessa and Ser Niños had the exact same `data-work`/`data-work-en` sentence ("Estrategia de marketing digital, pauta digital y optimización del sitio web.") — reworded Ser Niños' to remove the literal duplication, synced across `index.html` and `portafolio.html`. (3) **English residue in Spanish UI**: the case-study lightbox's `Work`/`Capabilities` labels were untranslated English inside an otherwise-Spanish page — changed to "Trabajo realizado" and "Capacidades" respectively across `index.html` and `portafolio.html` (this was flagged, not fixed, in a prior audit — see 2026-08-19 entry above re: "the lightbox's 'Work'/'Capabilities' UI labels still in English"); also added the new "Trabajo realizado" string to `translations.en`. Also removed 3 stray inert `data-i18n` attributes on `servicios.html`'s nav (that page has no language toggle wired up at all — dev-copy residue from copying the header markup off `index.html`). (4) **"ABRIR/CERRAR EL TALLER" door toggle**: audited — works correctly (`aria-expanded` flips, CSS `display:none` on the inactive label means only one is ever in the DOM/a11y tree at a time); no bug found, left as-is. (5) **Desktop/mobile nav duplication**: audited — `.nav-links` and `.nav-mobile-menu` are mutually exclusive via `display:none` at each breakpoint (plus `visibility:hidden`/`pointer-events:none` before `.is-open`), so only one is ever focusable/announced; no bug found. (6) **ES/EN toggle**: clicked through in-browser (not just read from the changelog) — round-trips ES→EN→ES correctly including the newly-unified CTA text; left untouched, works. (7) **CTA text unification**: replaced every primary "start a project" CTA ("Iniciar un proyecto", "Empecemos tu proyecto", "Cuéntanos tu proyecto (Formulario)", bare "Hablemos") with **"Hablemos de tu proyecto"**, and every secondary "see our work" CTA ("Ver trabajo") with **"Ver nuestro trabajo"**, across nav (desktop + mobile), hero, hub diagram, and contact section on `index.html`, `portafolio.html`, and `servicios.html` — applied only to the actual primary-contact/see-work CTA pair, not to unrelated links (e.g. `servicios.html`'s "Ver servicios"/portafolio's "Ver servicios" cross-page nav links were left alone, different role). Updated `translations.en` to match (added "Hablemos de tu proyecto"/"Ver nuestro trabajo", removed now-orphaned old-copy keys). (8) **Project form**: added the missing "¿Qué necesitas?" `<select>` field (Estrategia/Branding/Web·Ecommerce/Marketing·Growth/No estoy seguro), following the existing `pfBudget` select's exact markup pattern, plus `name` attributes on every field (email/name/company/need/budget/details) so the payload is ready whenever a backend gets wired up (still doesn't submit anywhere — known pending item, untouched). Updated the thank-you message from the vague "¡Gracias! Recibimos tu mensaje y te contactaremos muy pronto." to "¡Gracias! Te responderemos en un máximo de 1 día hábil." Applied identically to `index.html`, `portafolio.html`, `servicios.html`. (9) **Modal focus trap**: neither the case-study lightbox nor the project-form modal had one (Escape-to-close already worked, but Tab could escape to background content) — added a small shared `createFocusTrap()` helper (cycles Tab/Shift+Tab within the open modal, restores focus to the trigger element on close) to all three pages, wired into both modals where each page has them (`servicios.html` only has the form modal, no lightbox). (10) **Audits with no fix needed**: hero "Ver trabajo→Ver nuestro trabajo" CTA (already confirmed pointing at `#trabajo`, not `#servicios`, per the 2026-08-19 fix earlier in this log — re-verified still correct); internal anchor links (`a[href^="#"]`) all resolve to a real element on `index.html`; sampled image/asset requests all returned 200 in-browser; no `console.error`s on any of the 3 pages at desktop/tablet/mobile widths. Verified in-browser (local static server, not `file://`, to get real relative-path loading): ES/EN round-trip, mobile burger menu open/close, form field rendering + native validation + submit + thank-you message, Escape-close + focus-return on both modal types, 375/768/1440px layouts (no overflow/overlap found), marquee `aria-hidden` count (13 hidden / 13 visible), no console errors beyond the pre-existing unrelated Canvas2D `willReadFrequently` performance warning (Matter.js falling-icons canvas, not touched). **Explicitly left for Phase 2**, not attempted here: the Ajá Waffles/Prepapp/Seed Capital near-duplicate `data-work` template phrasing (similar but not verbatim, judged a content-strategy call); Moosy/Epika Store's asterisk-restates-description taglines; any copy/positioning rewrite; wiring the lead-form backend; the `translations.en` orphaned-key cleanup and `ProfessionalService`/`headEn` JSON-LD copy audits already flagged pending from prior sessions.

**2026-08-19** — Content-only pass rewriting Labs (#labs) and the Servicios hub diagram (#servicios) around a new 3-Lab architecture, per client request ("Core Lab / DEFINE, Brand & Experience Lab / CREA, Insight Lab / CRECE" — no layout/component/order changes). Built a single official service inventory first (client approved it explicitly) to fix the pre-existing problem where the same ~26-30 services were named differently in 3 places (nav mega-menu, hub diagram, Labs service lists) — e.g. "Naming" existed in Labs/hub but had been silently dropped from the nav menu in an earlier session; "Digital Experience" appeared both as a Lab-level category name and as an individual service inside its own list; "Rebranding" vs "Rebranding Strategy" vs "Identidad de marca & Rebranding" were 3 names for one service. Core Lab's `<h3>`/`<p>`/service list, Brand & Experience Lab's, and Insight Lab's were rewritten in Spanish (previously untranslated English h3 fragments like "We understand & grow.") with `data-i18n` added so the existing EN-toggle dictionary can pick them up in a future pass — **no EN translation was added this round, out of scope for this content-only pass; toggling to English currently falls back to showing the Spanish text for these specific h3/labs-service labels until translated**. Each Lab's flat service-button list was trimmed to only its *principal* services (Core: 5, Brand & Experience: 10, Insight: 8 = 23 total) — internal sub-capacities (Propuesta de valor, Posicionamiento, Rebranding, Brand Guidelines, E-commerce, Google Ads, Meta Ads, TikTok Ads, GEO, SEO local & GBP, Investigación de keywords, Segmentación de audiencias) were deliberately left out of the visible pill/button lists in both Labs and the hub — they exist only as descriptive detail inside each principal service, not as separate clickable items, per explicit client decision to keep the hub as "a quick, understandable view of what WOW does, not an exhaustive inventory." The Servicios hub diagram's right column went from 6 category pills (Brand Strategy/Brand + Creative/Digital Experience/Growth Marketing/Paid Media/Marketing Intelligence) down to 3 (DEFINE/CREA/CRECE), and its left column from ~26 stale/duplicate pills down to the same 23-service inventory used in Labs, with fresh `data-desc`/`data-desc-en` one-liners (deliberately reworded, not copy-pasted from the Labs paragraph descriptions, per client ask to avoid literal repetition between the two surfaces). Academy Lab (`labs-panel-academy`) was explicitly left untouched — its 4 services (Consultoría IA, Formación en IA, Acompañamiento estratégico, Talleres de equipo) don't fit the DEFINE/CREA/CRECE model (they're team-enablement services, not brand-facing deliverables) and its future placement is an open decision, not resolved here. Mega-menu (`.nav-dropdown`), hero, Filosofía (`#filosofia`), Proceso (`#metodologia`), Trabajo, and Academy's own panel were all explicitly out of scope for this pass. Verified in-browser: no console errors, `.hub-col-left .hub-pill` count is 23, `.hub-col-right .hub-pill` reads DEFINE/CREA/CRECE, each Labs panel's `.labs-service-btn` list matches the approved inventory — `hubArrows`' connector-line JS and the mega-menu (untouched) both still read pills dynamically via `querySelectorAll`, so neither broke from the pill-count change.

**2026-08-19** — Rewrote the nav mega-menu (`.nav-dropdown-grid`) to use the same 23-service DEFINE/CREA/CRECE inventory as Labs/hub, closing the gap flagged in the entry above. 6 `nav-dropdown-col` category columns (Brand Strategy/Brand + Creative/Digital Experience/Growth Marketing/Paid Media/Marketing Intelligence) reduced to 3 (`DEFINE — Core Lab` / `CREA — Brand & Experience Lab` / `CRECE — Insight Lab`, dot colors reused from hub: lima/blue/orange), same `nav-dropdown-link` markup and `data-desc`/`data-desc-en` popup mechanism untouched — no JS/CSS changes, column count is CSS-grid-driven off content so 3 columns render automatically. Reused the hub's exact `data-desc` copy per service (same service = same description in both places now) rather than writing a 3rd variant. Caught and fixed one capitalization drift during verification: Labs had "Estrategia de Marketing Digital" (title case) vs hub/menu's "Estrategia de marketing digital" — normalized to the hub's lowercase form so `textContent` matches exactly across all three surfaces (verified via headless JS set-comparison: Labs/hub/menu's 23 service labels are now the identical set). `nav-dot-magenta`/`nav-dot-ink`/`nav-dot-cloud` CSS classes are now unused by the menu (only 3 of the original 6 dot colors needed) but left in styles.css — harmless, not cleaned up. **Found but explicitly left alone this round** (outside the "mega-menu only" scope): the `ProfessionalService` JSON-LD's `knowsAbout` array (line ~44) still lists old service names ("Brand Strategy", "Diseño web", "Desarrollo web") from before this taxonomy existed; the `translations.en` EN dictionary still carries ~11 now-orphaned keys (Propuesta de valor, Posicionamiento de marca, Estrategia creativa, Presentaciones, Diseño editorial y presentaciones, Optimización y automatización de marketing, Identidad de marca & Rebranding, Analítica e Insights, "…y mucho más", Diseño y desarrollo web, IA & Automatización) whose Spanish source text no longer exists anywhere in the page since the hub rewrite — dead entries, harmless but worth pruning in a later pass. Verified in-browser: no console errors, `.nav-dropdown-link` set (23) === `.hub-col-left .hub-pill` set (23) === `.labs-panel:not(.labs-panel-academy) .labs-service-btn` set (23), byte-for-byte.

**2026-08-19** — Content-only rewrite of Proceso (#metodologia), closing the last content-audit item from the DEFINE/CREA/CRECE rollout. Client explicitly kept the circular structure's visual order as-is (CRECE→DEFINE→CREA→back to CRECE, i.e. still starts at the old "Insight" position) rather than reordering to start at DEFINE — reasoning: forcing DEFINE-first risks reading as a linear "strategy→design→marketing" pipeline, which is exactly what the circular framing is meant to avoid; the point is that the cycle has no true start, and starting from what you already know (CRECE) rather than from zero is itself part of the pitch. Changed: (1) H2 from "Un ciclo continuo de insight, estrategia y experiencia" (a 4th ad-hoc vocabulary for the same 3 capabilities) to "Un ciclo que aprende, define y crece." (2) `method-num` labels from "01 — INSIGHT"/"02 — CORE"/"03 — BRAND + EXPERIENCE"/"↻ BACK TO INSIGHT" to "01 — CRECE"/"02 — DEFINE"/"03 — CREA"/"↻ CRECE" (also fixes "BRAND + EXPERIENCE" using `+` where the closed-off official name is "Brand & Experience Lab"). (3) All 4 step `h3`s were English fragments ("We understand.", "We define.", "We create.", "We learn & optimize.") with no `data-i18n` — replaced with Spanish and added `data-i18n` (same as the Labs h3 fix earlier this session; EN dictionary entries still pending, same caveat as below). (4) Rewrote all 4 step paragraphs and the intro: the old copy restated each Lab's *function* (already covered by Labs+hub+menu — "Identificamos oportunidades...", "Convertimos insights en estrategia...", "Transformamos la estrategia en marcas..." were near-paraphrases of the official Lab descriptions), the new copy describes the *transition* between steps instead — what each step receives from the previous one and hands to the next, so Proceso now explains the mechanism of the cycle rather than being a 4th restatement of the service inventory. Preserved the one phrase from the old intro the client called out as the section's strongest idea ("cada experiencia genera nuevos datos, comportamientos y aprendizajes que regresan a..."), folded into the new intro's closing sentence with "Insight" swapped for "CRECE". Verified in-browser: no console errors; `updateScrollScrubs()`'s rocket/scrub JS (`querySelectorAll('.method-step')`, `.method-num`) reads only classes/DOM position, never the label text, so nothing broke.

**2026-08-19** — Rewrote the hero subheadline (`.hero-sub`), the last visible piece of the site still describing WOW as "Estudio de diseño y desarrollo... con la IA como herramienta de asombro" — a pre-DEFINE/CREA/CRECE positioning that undersold the offer (named only 2 of 10 Brand & Experience Lab services, no mention of strategy or growth) and treated AI as decorative flavor text rather than the real CRECE service it now is. New copy: "Diseño, marca y marketing digital bajo un mismo techo. De la estrategia al crecimiento, construimos lo que tu marca necesita para avanzar." — echoes the client's original founding statement for this whole DEFINE/CREA/CRECE rollout without re-explaining the architecture (Labs/hub already do that) or turning the hero into a service list; AI deliberately dropped from the hero on client's call — it reads as more credible inside CRECE's own "IA / Consultoría en IA" pill than as an obligatory hero mention. H1 ("Hagamos maravillas que se sientan reales") and both CTAs left untouched — audited and confirmed still on-brand/accurate. Noted but explicitly not touched: the "Ver trabajo" CTA links to `#servicios` not `#trabajo` (functional mismatch, flagged for a future pass, out of scope for a content-only change); `<title>`/meta description/OG/Twitter tags and the `ProfessionalService` JSON-LD still carry the old "Estudio de diseño + desarrollo con IA" tagline — not updated this round (SEO/meta pass, different scope than visible hero copy). Verified in-browser: no console errors, `.hero-sub` textContent matches exactly.

**2026-08-19** — Repurposed Filosofía (`#filosofia`) from a "Lo que no negociamos" manifesto (4 generic agency claims — "Datos sobre opiniones", "Resultados sobre promesas", etc. — that duplicated CRECE/DEFINE content more weakly and could've been copy-pasted onto any agency's site) into a "qué es WOW y qué promete" section that complements the Hero (Hero = desire, this section = understanding) without re-listing the 23 services or re-explaining DEFINE/CREA/CRECE. New eyebrow "QUÉ ES WOW", new H2 "Tu marca no se construye por partes.", and a new intro paragraph (`<p class="toggle-intro">`, no matching CSS rule added — it inherits the site's default `body`/`p` styling, which already fits this section's light background/ink-colored text, so no stylesheet change was needed). The 4 toggle-switch items were rewritten from generic principles into 4 distinct promises of what integration gets you (dirección → conexión → velocidad → continuidad), each with a `<strong>` lead-in — client specifically caught and fixed an early draft where items 2 and 3 both said "nothing gets lost between disciplines/vendors" (redundant); final version differentiates them as "todo está conectado" (decisions flow into outputs) vs. "menos fricción, más avance" (speed from one team having full context). Item 2 also seeds "lo que construimos genera información para decidir qué sigue" — a deliberate link to Proceso's CRECE→DEFINE loop without re-narrating it. The decorative `.outline-word` watermark (previously "PENSAMOS", with a special `.ow-kern` span fixing a P-E-N kerning gap specific to that word) changed to "NOSOTROS" — same 8-letter length, `.ow-kern` span removed since it isn't needed for this word's letterforms; verified via `getBoundingClientRect()` that it doesn't overflow the viewport at desktop width. Verified in-browser: no console errors, all new copy present via DOM read, `data-i18n`'s innerHTML-capture mechanism confirmed working fine with the new nested `<strong>` tags (same pattern already used elsewhere with `<em>`).

**2026-08-19** — Fine-tuned Proceso (#metodologia) copy after re-auditing it against the newly-written Filosofía section (whose "Todo está conectado" block now overlaps conceptually with what Proceso was saying, even though neither repeats the other's exact wording). Kept step 01, the loop-closing step (↻), and the intro paragraph exactly as they were from the earlier Proceso pass — client judged them solid. Changed only: H2 from "Un ciclo que aprende, define y crece." (functional but flat, closer to a checklist of the three words than a point of view) to "De lo que vemos a lo que hacemos. Y de vuelta." — communicates the loop with more personality without restating DEFINE/CREA/CRECE literally. Steps 02 and 03's `h3`/`p` rewritten because they'd drifted into mini service-lists again ("decisiones: posicionamiento, estrategia, prioridades" for DEFINE, "marcas, experiencias y puntos de contacto" for CREA — both near-duplicates of the official Lab descriptions in Labs/hub): 02 is now "Damos dirección a lo que descubrimos." / "Lo que aprendemos se convierte en decisiones claras sobre qué hacer, qué priorizar y hacia dónde movernos."; 03 is "Lo llevamos al mundo." / "Esa dirección toma forma, encuentra a las personas y empieza a generar nuevas respuestas." — both now describe movement/transition rather than capability. Also added the Lab name to all 4 `.method-num` labels per explicit client request ("super importante que pongas el nombre del lab") — "01 — CRECE" → "01 — CRECE · Insight Lab", "02 — DEFINE" → "02 — DEFINE · Core Lab", "03 — CREA" → "03 — CREA · Brand & Experience Lab", "↻ CRECE" → "↻ CRECE · Insight Lab". Checked this against the fixed-width circular-mode cards (`.method-step`, `width: var(--card-w)`, clamp 220–340px) since `.method-num` is `display:block` with no `white-space:nowrap` — confirmed via `getBoundingClientRect()` at 1500px viewport that all 4 cards render at the same 270px width with no horizontal overflow (rightmost edge 1364.8px, well inside 1500px viewport); the longer labels just wrap to 2 lines within the existing card height budget, no CSS changes needed or made. Verified in-browser: no console errors.

**2026-08-19** — Content audit of Trabajo (#trabajo) found its one real architecture inconsistency: `data-capabilities` (rendered as pill tags in the case-study lightbox via `dataset.capabilities.split('·')`) still used the pre-DEFINE/CREA/CRECE 6-category taxonomy (Brand Strategy, Brand + Creative, Digital Experience, Growth Marketing, Paid Media) on all 21 projects that have a lightbox — the only surface left in the whole site still showing the old architecture to visitors. Client's call: keep those specific-capability tags as-is (they read as immediately understandable — "you know exactly what was done") and *append* DEFINE/CREA/CRECE + the matching Lab name as additional pills, rather than replacing them. Mapped old→new (Brand Strategy→DEFINE/Core Lab; Brand + Creative & Digital Experience→CREA/Brand & Experience Lab; Growth Marketing & Paid Media→CRECE/Insight Lab), deduplicating so a project with e.g. both "Growth Marketing" and "Paid Media" only gets one "CRECE · Insight Lab" pair appended, not two. Applied via 8 grouped edits (exact `data-capabilities="..."` string matches, `replace_all` where the same old value repeats identically across multiple projects — e.g. "Brand Strategy · Brand + Creative" alone covered 8 projects). Moonking (the one color-card project with no lightbox/`data-capabilities`) untouched, as it has no capabilities field to begin with. Verified in-browser: 21/21 lightbox projects checked programmatically for correct DEFINE/CREA/CRECE presence matching their old tags (0 mismatches), and confirmed via an actual lightbox open (Clínica del Cerebro) that `#lightboxTags` renders all of them as separate pills: "Brand Strategy | Brand + Creative | Digital Experience | Growth Marketing | Paid Media | DEFINE | Core Lab | CREA | Brand & Experience Lab | CRECE | Insight Lab". No console errors.

Also surfaced but **not actioned this round** (client only asked for the capabilities fix): several other Trabajo findings from the audit — near-identical `data-work` text repeated verbatim across unrelated clients (La Marquessa/Ser Niños/Caminos de la Vida share one sentence; Ajá Waffles/Prepapp/Seed Capital share a template), a few generic taglines that just restate the description with an asterisk (Moosy, Epika Store), the lightbox's "Work"/"Capabilities" UI labels still in English while the rest of the site is Spanish, and no CTA/link from the homepage to the existing `portafolio.html` SEO page.

**2026-08-19** — Follow-up fix on the Trabajo capabilities change above: client wanted each DEFINE/CREA/CRECE addition to render as one combined pill ("CREA - Brand & Experience Lab") rather than two separate pills, since `#lightboxTags` splits `data-capabilities` on `·`. Changed the separator between the DEFINE/CREA/CRECE word and its Lab name from `·` to `-` within each pair. **Caught a scope leak during this edit**: the first `replace_all` (targeting `DEFINE · Core Lab`) also matched Proceso's `.method-num` step label ("02 — DEFINE · Core Lab"), which uses the identical substring — reverted that one line immediately after noticing. Redid the CREA/CRECE passes scoped to `" · CREA · Brand & Experience Lab"` / `" · CRECE · Insight Lab"` (leading space+middot) specifically so they wouldn't match Proceso's `"03 — CREA · Brand & Experience Lab"` or `"01 — CRECE · Insight Lab"` / `"↻ CRECE · Insight Lab"` (which are preceded by an em dash, not a middot) — confirmed via grep afterward that all 4 Proceso step labels still read with `·`, untouched. Verified in-browser: opened the Clínica del Cerebro lightbox, `#lightboxTags` now reads "Brand Strategy | Brand + Creative | Digital Experience | Growth Marketing | Paid Media | DEFINE - Core Lab | CREA - Brand & Experience Lab | CRECE - Insight Lab" — 3 combined pills instead of 6 separate ones. No console errors from the change itself (one unrelated pre-existing 404 for `clinica-del-cerebro/pattern.webp`, expected — that project is one of the 9 without a real pattern asset, the collage code silently no-ops on it per existing documented behavior, not something this edit touched).

**2026-08-19** — Removed the Academy Lab panel (`labs-panel-academy`) from Labs entirely, per client decision after the Academy audit: Academy was never going to be a 4th Lab, and its "Consultoría IA" service was a straight duplicate of "IA / Consultoría en IA", already living officially in Insight Lab/CRECE (Labs, hub, and menu) with its own approved description. Deleted the whole panel block (label, tag, h3 "Donde el conocimiento se convierte en criterio.", intro paragraph, and all 4 service buttons: Consultoría IA, Formación en IA, Acompañamiento estratégico, Talleres de equipo). Confirmed before deleting that nothing structurally depended on a 4-panel layout: `.labs-panels` is `display:flex` with `flex:1` per child (no fixed grid-column count), and the accordion JS reads `document.querySelectorAll('.labs-panel')` generically with no index/count assumption — so dropping to 3 panels required no CSS/JS changes. Also confirmed Academy had zero references anywhere else in the file (Hero, Filosofía, Mega Menu, Hub, Proceso, Trabajo, footer, JSON-LD all clean) — the panel was fully self-contained, so removing it doesn't leave any dangling references. The `translations.en` dictionary still carries the 4 now-unused Academy keys (Consultoría IA/Formación en IA/Acompañamiento estratégico/Talleres de equipo + the panel's intro paragraph) — same category as the other orphaned dictionary entries already flagged as pending cleanup, not removed this round. Per explicit client instruction, **Formación en IA, Talleres de equipo, and Acompañamiento estratégico are not deleted as ideas** — they're real, non-duplicate WOW offerings (team enablement/training, distinct from the DEFINE/CREA/CRECE "build for your brand" services) — they're just not rendered anywhere on the page right now because no placement has been decided yet (explicitly not shoehorned into one of the 3 Labs, and no new section was created for them). Their copy is preserved here in the changelog so it isn't lost before a future session decides where they live. Verified in-browser: `.labs-panel` count is 3 (insight/core/brand), no console errors beyond the pre-existing unrelated `pattern.webp` 404 for Clínica del Cerebro.

**2026-08-19** — Implemented the 3 high-priority items from the final global content audit, nothing else touched (no translations.en, no `knowsAbout`, no design/layout/CSS/JS). (1) `.labs-intro` ("Nuestros Labs trabajan como un sistema integrado, en un ciclo continuo de...") still said "Insight → Core → Brand + Experience → Insight" — the one remaining sentence in the whole file that actively contradicted the closed DEFINE/CREA/CRECE architecture and Proceso's confirmed cycle order; changed to "CRECE → DEFINE → CREA → CRECE". (2) Replaced the old "Estudio de diseño + desarrollo con IA" / "Workshop of Wonders convierte ideas en experiencias... potenciados por IA" tagline everywhere it appeared in metadata — `<title>`, `meta[name=description]`, `og:title`, `og:description`, `twitter:title`, `twitter:description`, and the `ProfessionalService` JSON-LD `description` (7 spots) — with copy that matches the current Hero's positioning: title "Workshop of Wonders — Diseño, marca y marketing digital bajo un mismo techo", description/OG/Twitter/JSON-LD all reusing the Hero subheadline verbatim ("Diseño, marca y marketing digital bajo un mismo techo. De la estrategia al crecimiento, construimos lo que tu marca necesita para avanzar.") for consistency rather than writing a 3rd variant. (3) Fixed the Hero's "Ver trabajo" CTA, which linked to `#servicios` (copy-paste leftover, flagged in two prior audits) — now links to `#trabajo`. Verified via grep that no visible/live instance of the old metadata tagline or the old Labs cycle text remains (the only 2 remaining matches are the pre-existing dead `translations.en` keys for both strings — explicitly out of scope, not touched). Verified in-browser: `document.title`, the hero CTA's `href`, and `.labs-intro`'s textContent all read correctly, no console errors.

**2026-08-19** — Closed out the EN-translation debt accumulated across this whole session's DEFINE/CREA/CRECE rollout. Compared every live `[data-i18n]` element's `innerHTML` against `translations.en` via headless JS (`document.querySelectorAll('[data-i18n]')` vs `Object.keys(translations.en)`) to find the exact gap in both directions rather than guessing. Added 35 new EN entries covering all-new copy from this session: Hero subheadline; Filosofía/"Qué es WOW" (eyebrow, H2, intro, all 4 `<strong>` blocks); Labs (the corrected `.labs-intro` with CRECE→DEFINE→CREA→CRECE, all 3 panel h3/p pairs, new service labels — Estrategia de marca, Arquitectura de marca, Estrategia de negocio, Dirección de arte, Diseño gráfico, Ilustración, Automatizaciones de Marketing, IA / Consultoría en IA); Proceso (H2, intro, all 4 step h3/p pairs). Rewrote the outline-word entry from the old kerned "PE\<span\>N\</span\>SAMOS"→THINK to "NOSOTROS"→"US" to match the current Filosofía watermark. Deleted 41 orphaned keys whose Spanish source text no longer exists anywhere in `index.html` — verified programmatically, not by guessing: old hero-sub, old Filosofía manifesto (5 keys: CÓMO PENSAMOS + 4 "sobre" claims), the old kerned PENSAMOS key, old Labs intro (Insight→Core→Brand+Experience wording), old Insight/Core/Brand Lab paragraphs, the 2 Academy-panel keys ("Donde el conocimiento se convierte en criterio." + its intro paragraph) plus the 4 Academy service-label keys (Consultoría IA, Formación en IA, Acompañamiento estratégico, Talleres de equipo — removed per the literal rule "delete if the key has no live use in index.html"; the 3 non-duplicate offerings' Spanish text itself remains documented in this changelog's pending-items history, just not in the dictionary anymore), old Proceso H2/intro/4-step copy, and ~20 dead mega-menu/hub-diagram keys from before the DEFINE/CREA/CRECE menu rewrite (Propuesta de valor, Posicionamiento de marca, Estrategia creativa, Diseño editorial, Presentaciones, Diseño web, Desarrollo web, E-commerce, SEO local & GBP, GEO (posicionamiento en IA), CRO (long form), Automatizaciones, IA, Diseño editorial y presentaciones, Pauta digital, Creación de posts gráficos, Análisis de métricas, Reportería y dashboards, Investigación de keywords, Segmentación de audiencias, Optimización y automatización de marketing, Identidad de marca & Rebranding, Analítica e Insights, …y mucho más, Diseño y desarrollo web, IA & Automatización, Rebranding). Net result verified via JS: 77 keys total, 0 orphans (every key matches a live element), and every live `[data-i18n]` string not covered by a key is legitimately self-same in both languages (Naming, Go-to-Market, Web Design, Web Development, UX/UI, Paid Media, CRO, Marketing Intelligence & Reporting, Market & Consumer Insights, Labs, DEFINE, CREA, CRECE, Work, Capabilities, and the numeric budget ranges) — confirmed this is a deliberate/correct gap, not a miss. **Found and fixed one more residue while verifying `<title>`/meta in EN mode**: a separate hardcoded `headEn` object (JS, not the `translations.en` dictionary) that overwrites `<title>`/meta description/OG/Twitter tags specifically when toggling to English — still had the pre-rollout tagline "Design + AI-Powered Development Studio" / "...powered by AI", untouched by the earlier ES-only metadata fix since that only edited the head tags' default (Spanish) content. Updated both `headEn.title` and `headEn.desc` strings to match the new positioning (only the two string literals changed, no logic touched). Updated JSON-LD `knowsAbout` from the old `["Brand Strategy", "Diseño de marca", "Diseño web", "Desarrollo web", "Marketing digital", "SEO", "GEO"]` to `["Estrategia de marca", "Naming", "Identidad de marca", "Web Design", "Web Development", "UX/UI", "SEO", "Paid Media", "IA / Consultoría en IA", "Marketing Intelligence & Reporting"]` — 10 official service names spanning all 3 Labs, no invented services. **Caught one JSON-LD authoring bug before it shipped**: first pass wrote `&amp;` inside the `knowsAbout` array (HTML-entity habit from editing HTML attributes all session) — but `<script type="application/ld+json">` content is raw JSON text, not HTML, so `&amp;` would have been read literally as 5 characters instead of decoding to `&`; fixed to a plain `&` and re-validated by `JSON.parse()`-ing the live script tag's `textContent` in-browser. Verified in-browser end to end: `JSON.parse()` on the live JSON-LD succeeds; toggling `setLanguage('es')`→`setLanguage('en')`→`setLanguage('es')` round-trips correctly on title, hero-sub, Filosofía, Labs, and Proceso; Labs/hub/menu's 23-service set still matches exactly across all three surfaces (unaffected by this pass); zero references to Academy anywhere in the file; no console errors.

**2026-08-19** — Synced the 2 high-priority gaps found in the Trabajo/`portafolio.html` final audit. (1) `data-capabilities` on all 21 `portafolio.html` project cards updated to match `index.html` exactly — these had gone stale when the DEFINE/CREA/CRECE combined pill was added to Home's Trabajo cards earlier this session but never propagated to the standalone portfolio page; copied each project's exact current capabilities string from `index.html` via `grep`, no new copy written. (2) The `CollectionPage`/`ItemList` JSON-LD only listed 15 of the page's 21 real projects (stopped at Clínica del Cerebro) — added the 6 missing `ListItem`s (Centro del Sueño y Ronquido, Carlos Ravelo, Caminos de la Vida, La Marquessa, Ser Niños, Walkmate) at positions 16–21, same `CreativeWork`/description pattern as the existing 15. Verified in-browser: fetched both pages, parsed `index.html`'s DOM, and diffed every project's `data-capabilities` string against `portafolio.html`'s — 21/21 match exactly, 0 mismatches. `JSON.parse()` on the live `ItemList` script succeeds with `itemListElement.length === 21`. No console errors. Nothing else touched (no ES/EN toggle work, no CTA added, no other copy changed) — those remain open per the audit below.

**2026-08-19** — Content-only edits to `servicios.html` (client reversed her earlier "don't touch it" instruction, then reconfirmed she does want it edited — see conversation). Two changes: (1) each category's `.svc-cat-eyebrow` (previously the old 6-category names — Brand Strategy, Brand + Creative, Digital Experience, Growth Marketing, Paid Media, Marketing Intelligence) now shows the Lab name instead: "Core Lab" (Brand Strategy), "Brand & Experience Lab" (both Brand + Creative and Digital Experience — same Lab, repeats by design per client confirmation), "Insight Lab" (Growth Marketing, Paid Media, and Marketing Intelligence — same Lab, repeats 3x, also by design). Section `id`s, `<h2>`s, and all service cards untouched — only the eyebrow line changed. (2) Per follow-up client request, merged the Paid Media section into Growth Marketing: moved Paid Media's 5 `svc-card`s (Pauta digital, Creación de posts gráficos para pauta, Google Ads, Meta Ads, TikTok Ads) into `#growth-marketing`'s `.svc-grid` (now 12 cards total, appended after the original 7), deleted the now-empty `<section id="paid-media">` entirely, and removed the dangling `#paid-media` link from the top `.svc-jump` nav (5 links now instead of 6) — this last part wasn't explicitly requested but was a necessary fix, since leaving a jump-nav link pointing at a deleted anchor would have been a broken/silent no-op click. Growth Marketing's own `<h2>`/intro paragraph ("Crecimiento y posicionamiento...") kept as the surviving section's copy, per client's "déjalo en el de growth." **Not touched, flagged for awareness only**: the FAQ answer to "¿Qué servicios ofrece Workshop of Wonders?" still says "...y crecimiento (Growth Marketing, Paid Media, Marketing Intelligence)" as three separate items — now slightly stale since Paid Media merged into Growth Marketing, but wasn't part of what was asked this round. Verified in-browser: 5 `.svc-category` sections (was 6), `#growth-marketing` has 12 `.svc-card`s, no `#paid-media` references left anywhere in the file, 5 eyebrow labels read correctly (Core Lab / Brand & Experience Lab ×2 / Insight Lab ×2), no console errors. **This whole edit is separate from the DEFINE/CREA/CRECE service-inventory reconciliation still pending a decision** (see the audit table shared with the client comparing index.html's 23-service inventory against servicios.html's ~30 granular services) — no service names, descriptions, or the inventory itself were touched here, only category-eyebrow labels and the Paid Media/Growth Marketing merge.

**2026-08-19** — Trimmed and consolidated the `servicios.html` service list further, per client request. Removed 5 cards outright: "Desarrollo web" (Digital Experience — client said cut it, no merge target given, `<h2>`"Diseño y desarrollo web" left as-is even though it now only covers one of its two named disciplines, flagged below), "Google Ads"/"Meta Ads"/"TikTok Ads" (Growth Marketing — client wants only the umbrella "Pauta digital" card, not the 3 platform-specific ones), "Investigación de keywords" and "Segmentación de audiencias" (Marketing Intelligence — client wants these folded into SEO and Pauta digital's copy instead of standing alone). Folded the removed concepts into the surviving cards' descriptions: SEO's `<p>` now opens with "Investigamos las palabras clave correctas y..." (was just "Posicionamos tu sitio..."); Pauta digital's `<p>` now includes "...con la segmentación de audiencia correcta y mensajes coordinados..." (was "...con piezas y mensajes coordinados..."). Renamed "Automatizaciones" → "Automatizaciones de Marketing" and replaced its description — reused the exact copy already approved for this service on `index.html`'s Insight Lab/hub/menu ("Procesos que se repiten solos, para que tu equipo no tenga que hacerlo.") instead of writing a new variant, for cross-page consistency. Growth Marketing's `.svc-grid` went from 12 cards to 9; Marketing Intelligence from 4 to 2; Digital Experience from 4 to 3. Verified in-browser: all three grids read the expected card lists, SEO's new description confirmed, no console errors. **Flagged, not touched**: Digital Experience's `<h2>` still says "Diseño y desarrollo web" despite "Desarrollo web" no longer having its own card — a naming mismatch worth revisiting; the FAQ's "¿Qué servicios ofrece...?" answer still lists "Growth Marketing, Paid Media" as separate items from the earlier Paid Media merge, now further stale.

**2026-08-19** — Removed 2 more service cards from `servicios.html` per client request: "Estrategia creativa" (Brand + Creative) and "IA" (Growth Marketing), no replacement copy or merge target given for either. While in there, **fixed the `Service`/`OfferCatalog` JSON-LD**, which had drifted out of sync with the page across the two prior `servicios.html` edits (it still listed "Desarrollo web", "Google Ads"/"Meta Ads"/"TikTok Ads", "Investigación de keywords", "Segmentación de audiencias", and kept "Paid Media" as its own `OfferCatalog` instead of merged into Growth Marketing) — this was an omission in those earlier passes, not something newly requested, but left inaccurate structured data live so fixed it now: removed the 6 stale entries, merged the Paid Media sub-catalog into Growth Marketing's, renamed its "Automatizaciones" entry to "Automatizaciones de Marketing" to match the card, and removed "Estrategia creativa"/"IA" from it too. Verified in-browser: `JSON.parse()` on both live JSON-LD scripts succeeds, neither "Estrategia creativa" nor "IA" appears anywhere (cards or JSON-LD), Brand + Creative now 7 cards, Growth Marketing now 8, no console errors.

**2026-08-19** — Trimmed `servicios.html` from 22 to exactly 20 services, per client's target count. Removed "Brand Book" and "Estrategia de marketing digital" as standalone cards — client's reasoning: Brand Book is a deliverable of brand identity work, not a separate service, and "estrategia" is implicit across the whole offer rather than needing its own line item. Folded both ideas into neighboring copy instead of just deleting them outright: "Identidad de marca"'s description now ends "...con su Brand Book como manual de aplicación, para que se use siempre igual de bien." (Brand Book survives as a named deliverable, not a service); "Pauta digital"'s description now opens "...con una estrategia detrás: la segmentación de audiencia correcta y mensajes coordinados..." (carries the strategic framing without a dedicated card — the section's own intro paragraph already said "...estrategia digital diseñados para crecer de forma medible," left untouched since it already covers this). Synced the `Service.hasOfferCatalog` JSON-LD to match (removed both entries there too). Verified in-browser: exactly 20 `.svc-card` elements, JSON-LD `hasOfferCatalog.itemListElement` also has exactly 20 offers matching the visible list, `JSON.parse()` succeeds, no console errors.

**2026-08-19** — Removed "Automatizaciones de Marketing" from `servicios.html` (card + `hasOfferCatalog` JSON-LD entry), no replacement/merge given. Now 19 services, down from the 20 the client had just landed on. Verified in-browser: 19 `.svc-card`s, 19 JSON-LD offers, no console errors.

**2026-08-19** — Removed "Posicionamiento de marca" from `servicios.html` (card + JSON-LD, fixed the now-trailing comma on "Propuesta de valor" in `hasOfferCatalog`'s Brand Strategy sub-array). Now 18 services. Leaves the Core Lab/Brand Strategy section with only 1 card ("Propuesta de valor") in what's still a 2-column `.svc-grid` — visually it'll now show one card and empty space next to it, not touched since this was explicitly a content-only request, flagging for awareness. Verified in-browser: 18 `.svc-card`s, 18 JSON-LD offers, no console errors.

**2026-08-19** — Added 3 new services to `servicios.html`'s Brand Strategy/Core Lab section (card + JSON-LD each), resolving the 1-card layout imbalance flagged above: "Auditoría de marca", "Arquetipo y personalidad de marca" (client confirmed these two concepts should be one combined card, not split — archetype is the tool used to define personality, splitting them felt redundant), and "Misión, visión y valores". None of these existed anywhere on the site before — arrived at through a live back-and-forth narrowing a broad brand-strategy-discipline brainstorm down by first removing anything that actually belongs to Brand + Creative (tone of voice, tagline, brand story — those are creative/verbal-identity outputs) and then anything that belongs to Insight Lab (market/competitor research, buyer personas — those are audience-research territory), leaving 9 strategy-only candidates; client picked 3 of those 9 (didn't take Naming, Arquitectura de marca, Estrategia de negocio, Go-to-Market, Estrategia de precios, or Category design — those remain open candidates if the section needs to grow again). Now 21 services total. Verified in-browser: 21 `.svc-card`s, 21 JSON-LD offers, Brand Strategy section confirmed with all 4 cards in the right order, no console errors.

**2026-08-19** — Added "Configuración de analítica y tracking" and "Consultoría de datos" to `servicios.html`'s Marketing Intelligence section (card + JSON-LD each) — client flagged these as "súper importantes" after I pointed out the section covered analyzing/reporting data but not the upstream step of actually capturing it (GA4/Tag Manager/conversion events) or deciding what to measure in the first place. Now 23 services total. Verified in-browser: 23 `.svc-card`s, 23 JSON-LD offers, Marketing Intelligence section confirmed with all 4 cards, no console errors.

**2026-08-19** — Fixed the 2 staleness issues flagged in the previous audit pass. (1) Digital Experience's `<h2>` "Diseño y desarrollo web" → "Diseño web" (no longer has a "Desarrollo web" card since that was cut earlier), and its intro `<p>` "Diseñamos y desarrollamos los espacios digitales..." → "Diseñamos los espacios digitales..." — same reason. (2) The FAQ answer to "¿Qué servicios ofrece Workshop of Wonders?" (both the visible `.svc-faq-a` and the `FAQPage` JSON-LD, which had drifted since they're two copies of the same text) still described the old 6-category taxonomy ("Brand Strategy... Brand + Creative, Digital Experience... Growth Marketing, Paid Media, Marketing Intelligence") — updated to the 3-Lab framing: "estrategia de marca (Core Lab), diseño e identidad (Brand & Experience Lab) y crecimiento (Insight Lab)." Verified in-browser: both JSON-LD scripts still parse, `<h2>` reads "Diseño web", FAQ answer reads the new Lab-based text, no console errors.

**2026-08-19** — Client caught 2 more places `servicios.html` still had the old 6-category taxonomy that I'd missed when renaming the eyebrows earlier: the top `.svc-jump` quick-nav links, and the `name` field on each `OfferCatalog` sub-catalog in the `Service.hasOfferCatalog` JSON-LD. Both renamed to match the eyebrows exactly (Core Lab / Brand & Experience Lab ×2 / Insight Lab ×2). Caught my own mistake mid-edit: first pass wrote `&amp;` inside the JSON-LD `name` value (HTML-entity habit again) — same class of bug as the `knowsAbout` fix earlier this session — corrected to a plain `&` before saving. Verified in-browser: zero matches anywhere in the file for "Brand Strategy", "Brand + Creative", "Digital Experience", "Growth Marketing", or "Marketing Intelligence"; `JSON.parse()` succeeds; jump-nav and JSON-LD category names read identically (Core Lab, Brand & Experience Lab, Brand & Experience Lab, Insight Lab, Insight Lab); no console errors.

**2026-08-19** — Replaced the Lab-name eyebrows/jump-nav/JSON-LD category names on `servicios.html` (Core Lab / Brand & Experience Lab ×2 / Insight Lab ×2 — which client felt read as repetitive across 5 sections) with 5 distinct descriptive names, applied consistently across all three surfaces (`.svc-cat-eyebrow`, `.svc-jump` nav, and each `OfferCatalog.name` in the JSON-LD): "Estrategia de marca" (was Core Lab), "Identidad y diseño de marca" (was Brand & Experience Lab, Brand+Creative side), "Experiencia digital" (was Brand & Experience Lab, Digital Experience side), "Crecimiento y marketing digital" (was Insight Lab, Growth Marketing side), "Analítica y datos" (was Insight Lab, Marketing Intelligence side). Section `id`s, `<h2>`s, and all service cards untouched — only the category-label layer changed. Verified in-browser: eyebrows, jump-nav link text, and JSON-LD category names all read identically across the 5 sections, `JSON.parse()` succeeds, no console errors.

**2026-08-19** — Major reconciliation: client declared `servicios.html`'s 23-service inventory (after all the trims/additions this session — Propuesta de valor, Auditoría de marca, Arquetipo y personalidad de marca, Misión/visión/valores, Identidad de marca, Rebranding, Diseño editorial, Presentaciones, Packaging & Merch, Copywriting, Diseño web, UX/UI Design, E-commerce, SEO, SEO local & GBP, GEO, CRO, Pauta digital, Creación de posts gráficos para pauta, Análisis de métricas de sitios web, Reportería y dashboards, Configuración de analítica y tracking, Consultoría de datos) as WOW's **official** service list — superseding the inventory `index.html` had been using since the original DEFINE/CREA/CRECE consolidation (which had different names/items: Naming, Arquitectura de marca, Estrategia de negocio, Go-to-Market, Dirección de arte, Diseño gráfico, Ilustración, Market & Consumer Insights, Marketing Intelligence & Reporting, IA / Consultoría en IA, Automatizaciones de Marketing, Editorial & Presentation Design, Web Design, Web Development, UX/UI — none of which survive). Rebuilt all 3 surfaces on `index.html` to carry the exact same 23 services grouped the same way (DEFINE: 4, CREA: 9, CRECE: 10): Labs' 3 panel service-button lists, the hub-diagram's 23 `hub-pill`s (fresh `data-desc`/`data-desc-en` per service, reusing already-approved translations where the exact same string existed before, writing new ones for the net-new services), and the mega-menu's 3 columns (same copy as hub, per the established "same service = same description" convention). Also updated the `ProfessionalService` JSON-LD `knowsAbout` to 9 names drawn from the new inventory (was still referencing dead names like "Naming", "Paid Media", "IA / Consultoría en IA"). Then propagated the category-name change into Trabajo's `data-capabilities` on **both** `index.html` and `portafolio.html`: renamed the 5 old taxonomy tags to match `servicios.html`'s 5 final category names (Brand Strategy→Estrategia de marca, Brand + Creative→Identidad y diseño de marca, Digital Experience→Experiencia digital, Growth Marketing→Crecimiento y marketing digital, Paid Media→Crecimiento y marketing digital) via `sed` across both files — since Growth Marketing and Paid Media now collapse to the same name, several projects (e.g. Clínica del Cerebro, all the "Digital Experience"-tagged ones) ended up with a duplicate tag; wrote a small Python script (`py`, not `python3`/`sed`, per this machine's environment) to parse every `data-capabilities` attribute, dedupe its `·`-separated parts while preserving order, and write back — verified via regex afterward that no `data-capabilities` value repeats any segment. The `DEFINE - Core Lab`/`CREA - Brand & Experience Lab`/`CRECE - Insight Lab` architecture pills already appended to each project (from the earlier Trabajo capabilities pass) were untouched — this only renamed the older 6-category tags sitting alongside them. Verified in-browser: Labs/hub/menu's 23-service sets match exactly across all three; JSON-LD parses; Trabajo capabilities on `portafolio.html` match `index.html` byte-for-byte across all 21 shared projects (fetched and diffed live); no console errors on either page.

**2026-08-19** — Site-wide "same language" audit per client request, checking every `.html` file for taxonomy residue and language-toggle completeness. Found and fixed 2 real issues: (1) `translations.en` had 9 orphaned keys left over from the pre-`servicios.html`-sync inventory (Arquitectura de marca, Estrategia de negocio, Dirección de arte, Diseño gráfico, Ilustración, Estrategia de marketing digital, Automatizaciones de Marketing, IA / Consultoría en IA, plus a stray self-mapped "Estrategia de marca":"Estrategia de marca" entry) and was missing 17 real entries for the new inventory's Spanish-only service names — replaced the whole service-label block with 23 entries matching the current inventory exactly (self-same English terms like UX/UI Design and E-commerce included for completeness even though the `es===en` fallback already handled them correctly). (2) Bigger find: the 23 `labs-service-btn` buttons across all 3 Labs panels never had `data-i18n` at all — a leftover from an old convention ("service-pill lists were already English by design, left untouched") that no longer applied once Labs got its own Spanish-only service names this session. Hub and mega-menu's identical labels already translated correctly on toggle; Labs silently didn't. Added `data-i18n` to all 23 via `sed` (`class="labs-service-btn" data-open-form>` → `...data-open-form data-i18n>`, safe since that exact string only matches these buttons). Also confirmed via grep that no real project file (`index.html`, `portafolio.html`, `servicios.html`) contains any of the old 6-category taxonomy terms or "Academy" — the only match found was inside `.claude/worktrees/`, an unrelated stray agent worktree, not part of the deployed site. Verified in-browser: `translations.en` has 0 orphaned keys and 0 real missing keys (the 8 "missing" — DEFINE/CREA/CRECE/Labs/Work/Capabilities/budget ranges — are legitimately self-same, confirmed intentional); toggling to EN now correctly translates Labs' service buttons (e.g. "Propuesta de valor" → "Value Proposition"); Labs/hub/menu's 23-service sets still match exactly; `servicios.html`'s 23 service names match `index.html`'s hub-pills exactly (fetched and diffed live); no console errors on any page.

**2026-08-19** — Redesigned the lightbox's capabilities block per client request: instead of one static "Capabilities" eyebrow with every tag (old 6-category names + DEFINE/CREA/CRECE-Lab markers) dumped into a single flat pill row, the modal now renders **one eyebrow+pills group per Lab actually used on that project**, in DEFINE→CREA→CRECE order. Implemented in both `index.html` and `portafolio.html` (same lightbox JS, copied verbatim per existing convention): `#lightboxTagsBlock` changed from a fixed `<p class="lightbox-label">Capabilities</p>` + single `.lightbox-tags` div to an empty container built entirely by JS on each click. New `CATEGORY_TO_LAB` map (`Estrategia de marca`→DEFINE-Core Lab, `Identidad y diseño de marca`/`Experiencia digital`→CREA-Brand & Experience Lab, `Crecimiento y marketing digital`→CRECE-Insight Lab) splits each project's `data-capabilities` into its DEFINE/CREA/CRECE lab markers vs. its category-name tags, then builds one `.lightbox-lab-group` (eyebrow + pill row) per lab present, only including the category tags that actually belong to that lab — a project touching 3 labs (e.g. Clínica del Cerebro) now shows 3 separate groups instead of one 7-pill wall of text. Kept the old flat-fallback path (desc-split, generic "Capabilities" label) for any element without `data-capabilities` — confirmed this only applies to the hero-mosaic buttons (which never had capabilities data to begin with, unrelated pre-existing behavior, not something this touched). Added one small CSS rule (`.lightbox-lab-group + .lightbox-lab-group { margin-top: 14px; }`) for spacing between stacked groups — first style change in a while, done because the client explicitly asked for this restructuring, not incidental. Verified in-browser on both pages: Clínica del Cerebro renders 3 correctly-split groups (DEFINE: Estrategia de marca / CREA: Identidad y diseño de marca + Experiencia digital / CRECE: Crecimiento y marketing digital), Orbit renders 2, no console errors beyond the pre-existing unrelated `pattern.webp` 404.

**2026-08-19** — Fixed a real layout bug in the lightbox image collage (`ProjectCollage`), reported by the client with a screenshot showing visible empty gaps of dark background between tiles (e.g. Clínica del Cerebro's collage). **Root cause**: `.project-collage` was CSS Grid (`grid-auto-flow: column dense`, `grid-auto-columns: max-content`) — a grid column's track width is the MAX of every item dense-packed into it, so whenever a narrow item (e.g. a 238px photo) landed in the same column as a wide one above/below it (e.g. a 418px wordmark logo), the column track rendered at 418px and the narrow item — sized to its own 238px via inline `style.width`, not stretched — left a ~180px gap of exposed background to its side. Confirmed via `getBoundingClientRect()` before fixing: two stacked items at the same `left` with different `width`s, exactly matching the screenshot. **Fix**: switched `.project-collage` from `display:grid` to `display:flex` (row) and added a new `.pc-col` wrapper class (`display:flex; flex-direction:column`) — `ProjectCollage.render()` in both `index.html` and `portafolio.html` now explicitly groups its built pieces into columns in JS (a `.pc-span2` item fills a column alone; two regular items stack into one column together) and wraps each column in its own `.pc-col` div before appending to the container, instead of appending pieces flat and letting CSS Grid's dense algorithm auto-place them. Each column is now sized only by its own 1-2 items (flex content-sizing, no shared track), so a narrow/wide mismatch can no longer leave a gap. Also had to add `el.style.height` inline per item (previously relied on the grid's fixed 2-row track height, which no longer exists) — `height` was already computed per piece, just never applied to `el.style` since the grid row track handled it implicitly before. Hit the project's known browser-cache gotcha while verifying (documented in this changelog from 2026-08-12): `styles.css` changes didn't show up until forcing a refetch via `link.href = 'styles.css?v=' + Date.now()` in-browser — not a real bug, just local dev-server caching same as before. Verified in-browser on both pages after the cache-bust: Clínica del Cerebro's 4 columns now measure exactly 418/238/418/190px (matching their own widest member, not a shared 418px track), no console errors beyond the pre-existing unrelated `pattern.webp` 404 probes.

**2026-08-19** — Follow-up fix on the collage column-grouping from the entry above: client asked that collage cards always render "complete" — a leftover unpaired short item (odd total count of 1-row-height pieces) was being placed alone in its own column, rendering at half the height of every neighboring column (a visually short, incomplete-looking card) instead of filling the full 2-row height. Added a `promoteIfLone()` step in both `index.html`'s and `portafolio.html`'s `ProjectCollage.render()`: whenever a short item ends up alone in a column (either because it's followed by a `.pc-span2` item or because it's the trailing item after the loop), it's now stretched to the full `rowH*2+gap` height with its width recomputed proportionally from its own real aspect ratio (same clamp logic already used for logos elsewhere — never stretched/cropped, just scaled up like a normal tall item). Deliberately excluded the always-1:1 generated tiles (palette/pattern/tagline) from this promotion — those are square by explicit design intent (documented in existing code comments), stretching one into a tall rectangle would look wrong; only real photos/logos/type-specimen tiles get promoted. Verified via a synthetic 3-image test (odd count, forces exactly one leftover) on both pages: 2 images pair into a normal 190px-tall column, the 3rd renders alone at the full 396px height with proportionally scaled width (594px) — no half-height column. Re-verified Clínica del Cerebro (8 items, even count, no leftover) still renders its original 4 full-height columns unchanged — no regression. No console errors beyond the same pre-existing `pattern.webp` probes.

**2026-08-19** — Fixed a second real collage bug the client caught right after the flex-column-grouping fix above (screenshot showed Arlo's second row visibly cropped off, cut by the caption panel below). **Root cause**: switching `.project-collage` from CSS Grid to `display:flex` dropped `grid-template-rows: repeat(2, var(--pc-row-h))`, which had been implicitly forcing the container to always be exactly 2 rows tall regardless of how much space its flex parent (`.project-collage-wrap`) allocated. With plain flex and no explicit height, the container just took whatever cross-space the modal's layout gave it — sometimes less than 2 rows' worth — and `overflow-y:hidden` silently cropped the shortfall instead of showing it. **Fix**: added an explicit `height: calc(var(--pc-row-h) * 2 + var(--pc-gap) * 3)` (2 rows + the inter-row gap + the container's own top/bottom padding, since `box-sizing:border-box` means padding eats into a set `height` rather than adding to it — first attempt used `*2` not `*3` and was still 32px short) and `flex: 0 0 auto` (no shrink) so the collage always reserves full 2-row space, matching the old grid's non-negotiable sizing exactly. **Also hit, and want on record**: this project's known browser-cache gotcha (documented in this changelog from 2026-08-12) bit twice while verifying — plain reloads kept serving a stale `styles.css` even on `portafolio.html`, and swapping the `<link>`'s `href` to a `?v=timestamp` query *without waiting for its `load` event* left two competing stylesheets in `document.styleSheets` (old + new) whose conflicting `.project-collage` rules fought it out in cascade order, which is what actually caused the follow-up "3rd row" symptom seen mid-debugging — not a real code bug, an artifact of testing methodology. Fixed by waiting for the swapped `<link>`'s `load` event before re-testing. Verified in-browser on both `index.html` and `portafolio.html` (single clean stylesheet each): Arlo's tallest column's lowest item bottom now sits at or above the collage's own bottom edge (`fitsWithin: true` in both), no visible cropping; Clínica del Cerebro re-checked, still correct (no regression). No real console errors (all 404s traced to the pre-existing unrelated `pattern.webp` probes).

**2026-08-19** — Two more lightbox layout fixes chained off the collage-height fix above, both caught by the client from screenshots. **Bug 1 (overlap):** fixing the collage's own height (`flex:0 0 auto`, non-shrinking) left its parent `.project-collage-wrap` still shrinkable (`flex:1 1 auto`) — when a project's caption content was tall (e.g. Arlo, 2 stacked lab-groups), the wrap got squeezed by the flex layout while its non-shrinking collage child didn't budge, so the collage visually overflowed past the wrap's own bottom edge and covered the top of the caption panel (Arlo's first capability group's eyebrow, "DEFINE - Core Lab", was invisible — not deleted, just painted-over by a collage photo; confirmed via `elementFromPoint` returning the photo `<img>`, not the label, at that screen position). Fixed by making `.project-collage-wrap` also `flex: 0 0 auto`, matching its child exactly. **Bug 2 (silent clip, then explicit scroll the client rejected):** with both wrap and collage now fixed-height, a tall caption could push total modal content past `.lightbox-inner`'s `max-height:90vh`; first fix was `overflow-y:auto` on `.lightbox-inner` so overflow scrolls instead of vanishing — client explicitly said no to any vertical scroll in the modal, asked to shrink images/text instead so everything fits without it. Reverted to that ask: removed a redundant `.lightbox-lab-group + .lightbox-lab-group { margin-top: 14px }` rule (the parent `.lightbox-caption-col` already applies `gap: var(--space-3)` = 24px between stacked groups — my earlier addition was double-spacing them), and reduced `--pc-row-h` from 190px to 150px (collage images now render smaller in the lightbox, per client's explicit trade-off). Kept `overflow-y:auto` on `.lightbox-inner` rather than reverting to `hidden` — with content now fitting, auto shows no scrollbar at all (behaviorally identical to hidden when nothing overflows) but won't silently re-crop content if some future project's caption is even taller; this satisfies "no scroll" as experienced without reintroducing the original silent-crop failure mode. Verified in-browser on both `index.html` and `portafolio.html`: Arlo (2 lab-groups) and Clínica del Cerebro (3 lab-groups, the worst case on the site) both now have `scrollHeight === clientHeight` (no overflow, no scrollbar) at 791px viewport height; `elementFromPoint` over the first label now correctly returns the label element, not a collage photo; no console errors beyond the pre-existing unrelated `pattern.webp` probes.

**Pending / open items**: **Formación en IA / Talleres de equipo / Acompañamiento estratégico have no home yet** — real WOW offerings, intentionally left unplaced (not in Labs, no new section), needs an architecture decision in a future session. `translations.en` now has 4 more orphaned keys from the removed Academy panel, same cleanup bucket as the other dead entries already logged. hero `<title>`/meta description/OG/Twitter tags and `ProfessionalService` JSON-LD `description` still use the old "Estudio de diseño + desarrollo con IA" tagline — candidate for the same SEO/JSON-LD pass as `knowsAbout` below. "Ver trabajo" hero CTA links to `#servicios` instead of `#trabajo` — functional fix, not content. `ProfessionalService` JSON-LD `knowsAbout` list not yet updated to the new service inventory. ~11 orphaned keys in the `translations.en` dictionary from pre-rewrite Spanish strings, safe to delete whenever EN translations are tackled. EN translations for all the new Spanish content added across Labs/hub/menu/Proceso this session (h3 titles, step copy — hub/menu `data-desc-en` were done inline, but every `data-i18n`-tagged h3/p added this session has no `translations.en` entry yet) still outstanding as one batch. Academy Lab's placement (keep as-is, fold into one of the 3 Labs, or move elsewhere) still undecided.

**2026-08-19** — Added `portafolio.html`, second of the two SEO landing pages (servicios.html was first), same approach: wholly separate file, index.html untouched. Shows all 21 portfolio projects in one flat `.pf-grid` (no door-reveal teasing like home's condensed `#trabajo` — this page's whole purpose is to be the complete list), reusing the exact `.work-item` markup/data-* attributes from index.html so cards open the identical case-study lightbox. `ProjectCollage` + the lightbox open/close/edge-scroll JS copied verbatim (same DOM ids) rather than extracted into a shared file — kept this pass to two independent static pages, no build step. Own title/meta/canonical/OG/Twitter tags plus `BreadcrumbList` + `CollectionPage`/`ItemList` (one `CreativeWork` per project) JSON-LD. Reuses servicios.html's `.svc-hero`/`.svc-contact` styles and header/mobile-menu/footer/project-form-modal; same hardcoded `.is-scrolled` nav fix (solid purple header from the start — this page's hero is light too). Registered in sitemap.xml. Verified in-browser: no console errors, 21 items render, Tin-T's lightbox collage renders 28 items with none broken, contact modal works, header color matches index.html.

**2026-08-19** — Added `servicios.html`, a standalone SEO landing page for services — first of two planned (portafolio next). Client explicitly does not want `index.html` touched ("no quiero editar el home, me encanta"), so this is a wholly separate file rather than a new route carved out of the single-page site; reuses the existing design system (styles.css, same header/mobile-menu/footer/contact-CTA/project-form-modal markup+JS copied verbatim) instead of building a new component system. Own `<title>`/meta description/canonical/OG/Twitter tags plus `BreadcrumbList` + `Service`/`OfferCatalog` + `FAQPage` JSON-LD. Content follows the site's existing 6-category service taxonomy (same categories as index.html's mega menu/hub diagram: Brand Strategy, Brand + Creative, Digital Experience, Growth Marketing, Paid Media, Marketing Intelligence) but each category gets an expanded intro paragraph not present on the home page, plus a 6-question FAQ accordion — avoids reading as thin/duplicate content next to the home page's condensed version. Deliberately left out of scope for this first pass: EN/ES toggle, lightbox, mega-menu dropdown, matter.js falling-icons canvas. New CSS prefixed `.svc-` in the shared styles.css to guarantee no collision with index.html's rules. Registered in sitemap.xml (`priority 0.9`). Verified in-browser (no console errors, FAQ accordion, contact modal, mobile burger menu — desktop and 375px).

**2026-08-18** — Client reported the collage still "tarda mucho en cargar" (takes a long time) after the stuck-loading fix below — the fix only addressed the *feeling* of being stuck, not the actual fetch time. Root cause: the `extra-*.webp` files added for the new portfolio images were encoded at up to 1600px on their long side, several times bigger than the collage ever renders them (tiles cap out around 2 grid rows tall — 190px desktop / 130px mobile each, so ~400px max even for a tall tile). Some projects' lightboxes were pulling 1.3-2.4MB total on a single click (Tin-T, Seed Capital, Pretty Pets worst offenders). Re-encoded every `extra-*.webp` (the new ones from the entry below, plus a handful of oversized pre-existing ones in pretty-pets/indeleble) at max 900px / webp quality 75-78 — generous headroom for retina at actual display size. Cut per-project payload roughly in half to two-thirds (Tin-T 1.5MB→0.8MB, Pretty Pets 2.4MB→0.9MB). Verified no visible quality loss and no broken images via `naturalWidth`/`naturalHeight` checks in-browser.

**2026-08-18** — Fixed the lightbox collage feeling "stuck"/frozen when switching between projects, reported right after the entry below added several brands' image counts up into the 15-25 range. Root cause: `ProjectCollage.render()` only did `container.innerHTML = ''` right before the final DOM build, *after* every image — including the tagline/pattern probe fetches, which ran sequentially one after the other instead of in parallel with the main set — had finished loading, so clicking a new project left the *previous* project's collage sitting fully rendered and static on screen for the whole fetch. Now clears the container and shows a spinner (`.project-collage.is-loading::after` in styles.css) synchronously on click, runs the tagline/pattern probes in parallel with the main image `Promise.all` via a shared `slugGuess` computed up front, and added a `renderToken` counter so a slow, still-in-flight render from a fast double-click doesn't finish after a newer one and overwrite it. Verified via headless JS: `is-loading` is `true`/0 children synchronously right after a click, before any image promise resolves.

**2026-08-18** — Added new client-supplied portfolio images (product mockups, packaging, social-post grids, lifestyle shots) to 13 brands' `ProjectCollage` sets. Client had just renamed the raw ChatGPT-generated assets in `~/Desktop/Fotos para sitio/Para Portafolios/<BRAND>/` from generic `ChatGPT Image ...`/UUID filenames to `Brand_Description.png`; converted the new files per brand to webp (`design-system/portfolio/<slug>/extra-N.webp`, continuing each folder's existing numbering) and spliced their paths into every `data-images` array for that slug (hero-mosaic `.media-card` ×3 duplicated marquee entries + the `.work-item` case-study card) via a one-off Python script, inserted right after each array's first (hero-photo) entry. Brands touched: Ajá Waffles, Arlo, Clínica del Cerebro, Epika, Geco, Indeleble, Lámparas Milán, Moosy, Prepapp, Pretty Pets, Seed Capital, Tata's Photos, Tin-T! (Tin-T got the most — 20 new product/mockup images from its Aguacatico/Cafecito character line). Verified in-browser via headless JS (`img.naturalWidth` check on every collage item after opening the lightbox) — no broken images, e.g. Tin-T's collage now renders 25 items, Seed Capital's 19.

**2026-08-17** — Client reported the site "stretching" on very wide/short-ratio viewports (ultrawide "gamer" monitors, or a normal window after zooming out with Ctrl -), sharing a screenshot of a mostly-empty pink void under the "Lo que no negociamos" toggle list on a tall window as an example, plus a reference (Superside) showing space appearing at the page's *sides* instead. Two separate bugs, fixed together: (1) `.method` and `.toggle-section` both sized themselves off a bare `calc(100vh - var(--nav-h))` (one as `height`, one as `min-height`, both with their content vertically centered) — on a tall/short-ratio viewport this stretched the box far beyond its content's real height, same root cause as `.hero`'s existing `min(96vh, 960px)` cap, so applied the same pattern here: `min(calc(100vh - var(--nav-h)), 760or800px)`. (2) The real "gamer monitor" complaint was about *width*, not height — every full-bleed section shares one `--page-x` variable for its horizontal padding, previously `clamp(24px, 8vw, 160px)`, so past that 160px cap any extra viewport width just kept stretching the *content* (nav items, hero text/images, everything) wider and wider instead of turning into margin. Changed to `max(clamp(24px, 8vw, 160px), calc((100vw - 1600px) / 2))` — below ~1900px-wide viewports this is identical to before (the added term is negative, ignored by `max()`), but past that point it grows so total content width tops out around 1600px, with the extra space becoming visible side margins instead. Since every section already threads through this one variable, no per-section markup changes were needed. Verified via computed-style checks at 2560px/2040px/1800px/1440px/375px (screenshotting the embedded test browser above ~2048px hit what looks like a texture-size limit in the tool itself, not a real page bug — confirmed the actual DOM/computed-style values were correct at 2560px even though the screenshot capture was clipped).

**2026-08-17** — Perf pass after the client reported the site feeling slow to load. Found two real issues: (1) `matter-js@0.19.0` (79KB, loaded from jsdelivr's CDN) was a blocking `<script src>` sitting right before the site's single big inline `<script>` block — since it had no `defer`/`async`, *every* piece of site JS (lightbox, nav, language toggle, mega menu, hub diagram — none of which touch Matter at all) sat waiting on that one external fetch to finish before any of it could run, so on a slow connection the whole page could look loaded but not respond to a single click for seconds. Fixed by pulling the physics code (`design-system`'s falling-icons playground, `.icon-playground`) out into its own `initFallingIcons()` function and lazy-injecting the matter.js `<script>` tag only once that section is within 600px of the viewport (`IntersectionObserver`, falls back to loading immediately if unsupported) — the rest of the site's JS no longer depends on it at all. (2) The 8 hero-media marquee photos (`design-system/hero-photos/*-vertical.webp`) were full source resolution (~1024×1536) despite rendering at roughly 200×300px in the marquee column — resized to 700px wide (generous headroom for retina, ~3.5x the display size) and re-encoded at webp quality 80 with Pillow (`method=6`), cutting their combined weight from ~850KB to ~500KB (41% smaller) with no visible quality loss. Both changes verified in-browser: JS interactivity (lightbox, lang toggle) now fires before Matter.js has even started downloading; falling icons still initialize correctly once scrolled into view; resized images look sharp at both mobile and desktop marquee sizes.

**2026-08-17** — Round of client-requested tweaks on top of the tagline-card work below, same session: (1) tagline card now scrolls 3 spaced-out copies of the art through a card that stays a true 1:1 square (`piece.taglineGapPx`/6-image track in `index.html`, `.pc-tagline-scroll` in styles.css) — first attempt made the card itself grow wide to fit 3 copies, client wanted it square instead; (2) fixed the scroll images stretching off their real aspect ratio — a generic `.pc-item img { width:100% }` rule (class+element selector) had higher specificity than the single-class `.pc-tagline-scroll-img`, fixed by scoping the selector to two classes; (3) flipped `.pc-item-pattern`'s scroll direction from right-to-left to left-to-right (moved the buffer tile from the right edge to the left); (4) forced `portfolio/<slug>/icon.webp` tiles (Arlo, Epika, Prepapp, Seed Capital, Carlos Ravelo, Walkmate, Lámparas Milán — not every project has one) to a true 1:1 in the collage instead of following the logo clamp range, which only ever narrowed a square icon further; client asked for an even tighter "app icon" look after seeing this (tried, then said revert — left at the plain 1:1 fix, `.pc-item-icon` tight-padding class removed again); (5) fixed `sampleCornerColor` (used to color the tagline card's background/gap to match the art) picking up letterform pixels instead of the real background on taglines whose text bleeds to the very edge (Orbit's "O" touched the sampled corner) — now samples a 12×12 grid and takes the modal color instead of one fixed pixel; (6) increased `--page-x` (site-wide horizontal padding) from `clamp(24px,6vw,96px)` to `clamp(24px,8vw,160px)` per a reference screenshot (Superside) the client wanted more breathing room like; (7) `Hagamos <em>maravillas</em>` could break apart onto separate lines at some widths — joined with `&nbsp;` (same convention as the `data-tagline` NBSP pairs, and note the translation-dictionary *key* has to be the literal `&nbsp;` entity text, not a real U+00A0 char — `el.innerHTML` re-serializes a real NBSP back to the `&nbsp;` entity string when captured, so the dictionary key must match that serialized form, not the raw character); (8) increasing `--page-x` above then exposed a pre-existing bug on very wide screens (~1920px+): `.hero-content`'s flex-basis was a fixed 520px while the h1's font-size clamp keeps growing up to 1920px-ish, so past a certain width "que se sientan reales" no longer fit on one line at the larger font and wrapped into a 3rd line, leaving a dead gap before `.hero-media` (whose `margin-left:auto` pushes it right regardless of how little width the text column actually used) — fixed by making `.hero-content`'s flex-basis and the h1's max-width scale with viewport (`clamp(520px, 40vw, 800px)`/`clamp(640px, 40vw, 800px)`) instead of staying fixed. Verified all of the above in-browser at 1920px, 1366px, and 375px.

**2026-08-17** — Replaced the generated "tagline card" in `ProjectCollage` (the flat brand-color card that set `data-tagline` in huge type via canvas-measured JS text layout) with the client's own hand-designed tagline graphics. She designed one PNG per brand (`Tagline_<Nombre>.png`, sourced from `C:\Users\majot\Desktop\Fotos para sitio\Para Portafolios\<PROJECT>\`) and copied 13 of them into `design-system/portfolio/<slug>/tagline.png` (Aja Waffles, AM Studios, Arlo, Clínica del Cerebro, Geco, Indeleble, Lámparas Milán, Orbit, Prepapp, Pretty Pets, Seed Capital, Tata's Photos, Tin-T; Carlos Ravelo, Epika, Moosy, and Walkmate don't have one yet, so those cards are simply absent — same as before for projects lacking a piece). The collage now probes for that file by path (same technique as the existing `pattern.webp` probe, no extra `data-*` needed per work-item) and, if found, renders it as two copies side by side that loop sideways forever via `translateX(-50%)` (`.pc-item-tagline-img`/`.pc-tagline-scroll` in styles.css) — same animation technique as the pattern/palette tiles, just with the real image instead of generated text. Removed the entire old text-layout code path (`isPhrase`, `.pc-item-phrase`, `.pc-phrase-*` CSS, font measuring/word-splitting/accent-word logic) since it's now dead. `data-tagline`/`data-tagline-en` attributes are left in the HTML unused by this new path (still read by the lightbox description area itself, unrelated to the collage) — not cleaned up, out of scope for this change. Also fixed `.claude/launch.json`'s dev server config (`python` → `py`, the actual Python launcher available on this machine) so the local preview could run at all. Verified in-browser: Tin-T's card now shows her real "Recuerdos que cuentan historias" graphic scrolling, no console errors.

**2026-08-17** — Full re-audit of the EN/ES toggle after the client asked to "revisa todo nuevamente." Grepped every accented-character string and every `data-i18n`/`data-desc`/`data-category`/`data-work`/`data-tagline` pair in `index.html` for gaps. Found and fixed 2 real leaks: the 4 Academy Lab service buttons (Consultoría IA, Formación en IA, Acompañamiento estratégico, Talleres de equipo) had no `data-i18n` at all (every other Lab's service-pill list is already English by design, so this one — the only Spanish one — got missed), and the hub-diagram pill "Diseño editorial y presentaciones" had `data-i18n` but no matching dictionary entry (the mega-menu has the same two concepts as separate buttons/keys, "Diseño editorial" and "Presentaciones", which masked that the combined phrase used here was never added). Also added the missing `data-i18n-aria-label-en` on the 4 Labs panel "Ver más" toggle buttons (accessibility-only, not visibly rendered, but every other icon-only control on the page already had its aria-label translated). Everything else checked out: all 113 `data-desc`/17 `data-tagline`/21 `data-work` pairs have matching `-en` counterparts; the 21 `data-category` values without an `-en` pair are all already English source text (e.g. "Lighting · Home & Decor"), which is correct and not a gap.

**2026-08-17** — Follow-up to the language toggle below: the 5 giant decorative section watermarks (`.outline-word` spans — PENSAMOS, SERVICIOS, PROCESO, TRABAJO; LABS is already the same in both languages) were `aria-hidden="true"` and had been skipped by the original pass since they're not read by screen readers, but the client flagged them as visibly untranslated giant background text. Added `data-i18n` to all 4 (the mechanism keys off exact `innerHTML`, so the PENSAMOS span's nested `<span class="ow-kern">N</span>` kerning wrapper is preserved as part of the Spanish key, mapped straight to plain `"THINK"` in English) and their EN values (SERVICES/PROCESS/WORK) to the `translations.en` dictionary. Verified both directions via headless JS (`setLanguage('en')`/`setLanguage('es')`, read `.outline-word` textContent) — no changes to layout/CSS, additive only.

**2026-08-17** — Added a full-site EN/ES language toggle (client explicitly chose a same-page toggle over a separate `/en/` page or partial translation). A small "ES / EN" pill sits in both `.nav-links` (desktop) and `.nav-mobile-menu` (mobile), styled with the existing purple/lima palette (`.lang-toggle` in styles.css). Two mechanisms cover the whole page: (1) any element carrying `data-i18n` gets its original Spanish `innerHTML` captured once into `data-i18n-es`, then swapped against a flat `translations.en` dictionary keyed by that exact Spanish text — covers headings, buttons, nav labels, the ~55 mega-menu/hub-diagram service button labels, form labels/placeholders/options, and footer/modal copy; (2) every element that carries a dynamic `data-desc`/`data-category`/`data-tagline`/`data-work` attribute (read live by the hub-popup, mega-menu-popup, and lightbox click handlers already on the page) got a matching `<attr>-en` value baked into the HTML at build time, and `setLanguage()` overwrites the *base* attribute itself on toggle (backing up the Spanish original into `<attr>-es` first) — so none of the existing JS that reads `el.dataset.desc` etc. had to change at all; it just always reads whichever language is currently active, including values read later at click/hover time. Covers all 22 Trabajo project cards (category was already English in the source copy; description/tagline/work narrative translated), all 30 mega-menu items + 26 hub-diagram pills (both label text and popup description), hero eyebrow/h1/sub/CTAs, the "Cómo pensamos" toggle list, Labs intro + body paragraphs (headings/service-pill lists were already English by design, left untouched per existing convention), Proceso step copy, the lead-form modal and lightbox chrome, and the footer. `<html lang>` and `<title>`/meta-description/OG/Twitter tags update too. Preference persists in `localStorage` (`wow-lang`, wrapped in try/catch so it degrades gracefully if storage is unavailable), defaults to `es` on first visit (no browser-language auto-detect, per requirement). **Gotcha found**: the `data-tagline` values use literal U+00A0 (non-breaking space) between words per the 2026-08-16 convention — a first pass building the English `data-tagline-en` map used regular ASCII spaces as dictionary keys and silently matched almost nothing (16 of 17 taglines got no `-en` attribute at all); fixed by extracting the exact NBSP-containing strings from the file itself and pairing them positionally with hand-written English translations (also NBSP-joined) instead of re-typing the Spanish keys by hand. Verified via headless JS execution (click `#langToggle`, `#langToggleMobile`, a hub pill, a work-item lightbox, then back to ES) — nav/hero/labs/hub-popup/lightbox/footer/form all swap correctly both directions, no console errors, no mismatched HTML tags (checked with `html.parser`).

**2026-08-16** — Added a new `pc-item-pattern` tile to the `ProjectCollage` lightbox: the brand's own real seamless print pattern (client supplied `Patron_*.png` files per brand in `~/Desktop/Fotos para sitio/Para Portafolios/<BRAND>/`), tiled and scrolling sideways forever. Only 8 of the 17 real-photo brands had a pattern file — copied+converted to `design-system/portfolio/<slug>/pattern.webp` for: Epika, Geco, Indeleble, Lámparas Milán, Orbit, Prepapp, Seed Capital, Tata's Photos. The other 9 (Ajá Waffles, AM Studios, Arlo, Carlos Ravelo, Clínica del Cerebro, Moosy, Pretty Pets, Tin-T, Walkmate) only had logo/typography/flyer-mockup assets in that folder, no actual pattern — client confirmed to leave those without the tile rather than fake one from the logo. `ProjectCollage.render` probes for the pattern file directly by path (`design-system/portfolio/<slug>/pattern.webp`, same slug the type/color lookups already use) instead of requiring it in each work-item's `data-images`, so it silently no-ops for the 9 without one. First pass rotated the tile 360° continuously, but client wants it to stay upright/recognizable and scroll horizontally instead — switched to `translateX` by exactly one background-tile-width (`--pattern-tile-w`, computed in JS as `rowH * 0.3` and also used as the CSS keyframe's exact translate distance) so the loop has no visible seam.

**2026-08-16** — SEO pass on the base HTML (the site had only `<title>` + meta description before this): added `<link rel="canonical">`, `robots` meta, `theme-color`, full Open Graph tag set (title/description/image/url/site_name/locale), Twitter Card tags, and a `ProfessionalService` JSON-LD block (name, url, logo, sameAs Instagram/LinkedIn, knowsAbout service list) — all pointing at the live URL `https://wow-landing-beta.vercel.app/`. Generated `design-system/og-image.jpg` (1200×630, brand purple `#380757` background with the white logo centered) since there was no existing social-share image. Added `robots.txt` (allow all, points at sitemap) and `sitemap.xml` (single URL — it's a one-page site) at the repo root. Audited all 71 `<img>` tags — every one already had a real `alt`, no changes needed there. Heading structure was already clean (single `<h1>`, `<h2>` per section) — left as-is.

**2026-08-16** — `79bcdd7` Added a new animated tagline card to the `ProjectCollage` lightbox, alongside the existing kinetic-type and palette tiles: `pc-item-phrase`, a flat single-brand-color card showing the project's real tagline (sourced from the client's brand PDF `Portafolio de marcas.pdf`, added as `data-tagline` on 13 real work-items; falls back to `data-desc` — also now grouped for the 4 items that had no PDF source: Moosy, Epika Store, Carlos Ravelo, Walkmate) set huge/bold in the brand's own primary typeface (same `TYPE_FONT_BY_SLUG` lookup the type tile uses), starting flush at the top of the card. Line breaks are controlled explicitly via the `data-tagline` string itself — join words with a literal U+00A0 (non-breaking space) to keep them on one line, wrap a word in `*asterisks*` to force it as the accent word — rather than relying on the browser's auto-wrap, which was breaking one word per line and reading as too many jumps. Every tagline was hand-grouped down to 2-3 lines max for readability. The accent word/line renders in the brand's secondary typeface (upright, not italicized — falls back to italic Playfair Display only when a project has no secondary face); its color auto-flips light on dark-sampled brand backgrounds (luminance-checked) so it doesn't go invisible on e.g. Tata's Photos' black. The whole block repeats vertically ×2 (not more — each repeat adds a full set of line breaks) and the two block copies loop sideways forever for the "always full, always moving" effect. Font size per card is computed from the longest line's character count (not a fixed fraction of row height) so a long unbreakable NBSP-joined line always fits its box instead of overflowing and getting clipped by the card edge. Hit and fixed one real encoding bug along the way: an early heredoc-based edit corrupted the "í" in Pretty Pets' tagline into a replacement byte — rewritten via a UTF-8-safe Python script instead of inline bash heredocs for all subsequent tagline edits. Follow-up same day: the fixed `REPEATS = 2` left a visibly empty gap at the bottom of cards with short taglines (e.g. Ajá Waffles) while looking fine on longer ones — replaced with a computed repeat count (`Math.ceil((rowH * 1.15) / (words.length * lineHeightPx))`, minimum 2) so the block always fills-and-slightly-overflows the card's actual height at its fitted font size, regardless of how many lines the tagline itself grouped into. Second follow-up: the font-size fit was estimating the longest line's width with a fixed 0.62-per-character guess, which ran narrower than the brand's real font in several cases and left a visible empty gap on the right edge instead of bleeding past it like the left edge did. Replaced the guess with an actual measurement — render each line into a hidden absolutely-positioned span in the brand's real font at a 100px reference size, read its `getBoundingClientRect().width`, and scale off that — so the widest line always reaches (and slightly bleeds past) both edges. Third follow-up, matching a specific reference screenshot the client sent: bumped the size ceiling (`rowH*0.19` → `rowH*0.4`) and the width-fit bleed factor (`*1.02` → `*1.22`) so the widest line now visibly crops into both edges rather than just touching them, and switched `.pc-phrase-text`/`.pc-phrase-line` from left-aligned to `text-align: center` (dropping the horizontal 6% padding) so that overflow crops symmetrically on both sides instead of only bleeding right. Fourth follow-up: client asked for left-alignment back ("obligatorio alineado a la izquierda") — reverted `text-align` to `left`. Fifth follow-up: client then clarified they didn't want any cropping at all — the `*1.22` bleed factor was cutting off the widest line's edge; changed to `*0.97` so the widest line fills right up to (not past) both edges with a small safety margin, no clipping. Sixth/seventh follow-up: client asked to speed up the horizontal loop (16s → 7s → 3s), then landed on an explicit `10s`. Eighth follow-up: client asked for short connector/function words (la, lo, un, tu, de, con, que, en, y, etc.) to break onto their own line instead of NBSP-joined to a neighboring word — rewrote all 17 `data-tagline` values accordingly. Immediately reverted (ninth follow-up, same session): client clarified they did NOT want that after all — `git checkout 52c158f -- index.html` restored the pre-connector-split grouped taglines (Descubre / LA SEMILLA / DEL ÉXITO, etc.), while keeping the 10s loop-speed change in `styles.css` from the same round.

**2026-08-15** — Added client-supplied orientation-matched photos (vertical/horizontal/square, from `~/Desktop/Fotos para sitio/para el hero`) for the 15 real-photo projects, replacing the single generic image each used before. Goal: no card ever crops its cover image. Hero mosaic cards (`.media-card { aspect-ratio: 2/3 }`) now use the `-vertical.png` variant (1024×1536, exact 2:3 match — zero crop). Work-item cards (320×220, ≈1.4545:1) now use the `-horizontal.png` variant (1536×1024, 1.5:1 — near-zero crop). AM Studios and Arlo initially had no horizontal source file when this started; client supplied them mid-session as `HERO_HORIZONTAL_AM.png` and `HERO_HORIZONTAL_Arlo_2.png` respectively (Arlo's original `HERO_HORIZONTAL_Arlo.png` got replaced by a `_2` version in the source folder while this was in progress) — both copied in as `am-studios-horizontal.png` / `arlo-horizontal.png`. New files live in `design-system/hero-photos/` alongside (not replacing) the original single-image files, which are still used by the lightbox's `pool` fallback array (unaffected by orientation — `ProjectCollage` never crops) and anywhere else not covered by this pass. Square variants exist for 9 projects (am-studios, centro-sueno-ronquido, epika-store, geco, indeleble, prepapp, pretty-pets, seedcapital, tatas-photos) but aren't used anywhere yet — no current card shape calls for 1:1.

**2026-08-14** — Fixed the edge-hover auto-scroll (see entry below) pausing whenever the mouse hovered one of the `.pc-nav` arrow buttons — exactly where the client most needed it to keep going. Cause: the buttons are visually positioned over collageEl's edges but are DOM siblings of it inside `.project-collage-wrap`, so once the pointer moved onto a button (the topmost element there), the browser fired collageEl's own `mouseleave` even though the cursor was still within its visual bounding box. Moved the `mousemove`/`mouseleave` listeners from `collageEl` to `collageEl.parentElement` (the wrap, which contains both the collage and the buttons) so hovering a button no longer counts as leaving. Verified: hovering directly over `#pcNavNext` now keeps `scrollLeft` climbing (0 → 316px in 600ms) instead of freezing.

**2026-08-14** — Replaced the lightbox image collage's scrollbar with edge-hover auto-scroll + round arrow buttons, per client reference screenshot (a lime circular arrow button over a dark gallery). `.project-collage` (styles.css) now hides its scrollbar entirely (`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`). New markup: `.project-collage-wrap` wraps the collage plus two `.pc-nav` round buttons (`#pcNavPrev`/`#pcNavNext`, lima background matching the reference) positioned over its left/right edges, hidden via `.is-hidden` when there's nowhere left to scroll that direction (checked on scroll/resize via `updatePcNav()`). Also added a `mousemove` handler on the collage: getting within 90px of its left/right edge auto-scrolls that direction via `requestAnimationFrame`, speeding up the closer the cursor gets to the actual edge (up to 16px/frame) — no click or manual drag needed, matches "al acercar el mouse al borde se mueva". (An initial click-and-drag implementation was built first per "que el mouse como que lo arrastre" but replaced once the client clarified they meant edge-proximity auto-scroll, not manual dragging.)

**2026-08-14** — Portfolio restructuring per client request: "no quiero que se le pueda dar clic a los logos de las marcas, las marcas deben tener cada una su card en portafolio". (1) `.client-logo` marquee entries (`.work-brands-track`) converted from `<button data-lightbox>` to plain non-interactive `<div>` — clicking a logo no longer opens the lightbox; kept the `data-match` cross-highlight hover effect (mouseenter/mouseleave only, not a click, so it doesn't imply interactivity) for the pairs that still have it. Removed `cursor:pointer` from `.client-logo`. (2) The marquee's scroll animation no longer pauses on hover (`.work-brands-marquee:hover` rule removed) — no longer needed since there's nothing to stop and read. (3) Added 5 new portfolio cards for brands that only had a logo before (Clínica del Cerebro, Carlos Ravelo, Caminos de la Vida, La Marquessa, Ser Niños) using their existing real desc/work/capabilities data. Since their only asset is a small logo (not a project photo) and `.work-item img` uses `object-fit:cover` (would crop/distort a logo), gave them a new `.work-item-logo` variant instead: logo on a white contrast badge over a brand-colored gradient, name + category below — client picked this over waiting for real photos. (4) Client then asked to remove the cards for Bary, Bilac, Tiny, Amlogo, and Acústica Eafit specifically — done, those brands (except Acústica Eafit, which had no logo) are back to marquee-only with no card and no `data-match`. (5) `ProjectCollage.render` (the lightbox image-collage system) no longer lets any image span both rows (`.pc-span2`) *except* when there's exactly one image in the set (nothing to share the strip with) or the image itself is portrait/vertical (ratio < 0.85) — landscape/square images in a multi-image set are now always single-row height. Dropped the old priority-based "hero image" tall-promotion entirely.

**2026-08-14** — Replaced alphabetical mega-menu ordering with priority-based ordering, per client input on their top services (Pauta digital, Diseño web, SEO, Creación de posts gráficos para pauta) plus UX reasoning about serial-position effect (users scan top-down, first 2-3 items in a column get disproportionate attention — alphabetical order wastes that on whatever starts with A/B). Each column now leads with its flagship/most-requested service, then closely related items grouped right after it (e.g. SEO → SEO local & GBP → GEO, all search-visibility; Pauta digital → Creación de posts gráficos para pauta, the strategy + its creative), with more strategic/niche items toward the bottom. Also added a "Brand Book" service (Brand + Creative, right after Identidad de marca — a natural deliverable of identity work that was missing from the list).

**2026-08-14** — Sorted each mega-menu column's services alphabetically (was ad-hoc order from however each was added over the session). Category order (Brand Strategy → Brand + Creative → Digital Experience → Growth Marketing → Paid Media → Marketing Intelligence) left as-is, only the items within each column were reordered.

**2026-08-14** — Cleaned up mega-menu redundancy flagged in a review: removed "Naming" (Brand Strategy), "Dashboards en Power BI" (redundant with "Reportería y dashboards"), "Diseño gráfico" (redundant umbrella over the specific Brand+Creative items), "Analítica e Insights" (redundant with the other Marketing Intelligence items, esp. "Segmentación de audiencias" which its own description already mentioned), and all three "Consultoría" entries (de marca / en marketing digital / con base en métricas). Merged "Packaging" and "Merch" back into one "Packaging & Merch" entry. Explicitly kept Google Ads/Meta Ads/TikTok Ads despite them overlapping with "Pauta digital" — client's call, they want those exact keyword strings on the page for their own SEO.

**2026-08-14** — Renamed "CRO" to "CRO (Optimización de la Tasa de Conversión)" and removed "Optimización de embudos" — the two had become redundant once CRO was added (both describing conversion optimization), client kept the spelled-out CRO entry.

**2026-08-14** — Trimmed and re-balanced the mega menu per client feedback right after the previous round: removed "Email & SMS marketing", "Manejo de redes sociales", "Marketing de contenidos" (Growth Marketing), "Producción gráfica y fotográfica" (Brand + Creative — client doesn't do photo shoots either, not just video), and "Investigación de mercado" (Marketing Intelligence). Added four data-driven services to Marketing Intelligence instead: "Dashboards en Power BI", "Análisis de métricas de sitios web", "Investigación de keywords", "Consultoría con base en métricas" — that column's now the client's actual analytics/data-strategy focus rather than generic market research.

**2026-08-14** — Rounded out the mega menu with several more service-gap additions from the client, each with its own popup description: Growth Marketing gained "Consultoría en marketing digital", "Marketing de contenidos", "GEO (posicionamiento en IA)", "Email & SMS marketing", "Manejo de redes sociales", and "CRO" (now 12 items, the longest column); Marketing Intelligence gained "Reportería y dashboards", "Investigación de mercado", "Segmentación de audiencias" (was a single-item column before); Brand + Creative gained "Estrategia creativa", "Diseño gráfico", and "Producción gráfica y fotográfica" — client explicitly asked for production *without* video, since they don't offer video services. Verified the dropdown layout still holds up with Growth Marketing at 12 items and Brand + Creative at 10 — no overflow, columns just grow to different heights as expected from CSS grid.

**2026-08-14** — Added two new services to the mega menu after discussing service-gap analysis with the client vs. what agencies typically offer: "SEO local & Google Business Profile" (Growth Marketing column, after SEO) and "Creación de posts gráficos para pauta" (Paid Media column, after TikTok Ads). Each with its own `data-desc` for the click-to-see-description popup.

**2026-08-14** — Split more combined mega-menu entries per client feedback: "Diseño editorial y presentaciones" → "Diseño editorial" / "Presentaciones"; "Packaging & Merch" → "Packaging" / "Merch". Also renamed "IA & Automatización" to just "IA" — the "Automatización" half was redundant now that "Automatizaciones" already exists as its own entry in the same column (Growth Marketing) since the previous split.

**2026-08-14** — Made each service in the mega menu clickable to show a short description, reusing the same white-card popup pattern as the `#servicios` hub-diagram pills (`.hub-popup`) but as a new `.nav-service-popup` positioned relative to `.nav-dropdown`. Converted the dropdown's `<a>` items to `<button data-desc="...">` (same content already used for matching hub-pills where one exists, new copy written for the split-out ones — Rebranding, Desarrollo web, Automatizaciones, Consultoría de marca). Two bugs fixed along the way: (1) the buttons were stretching to their column's full width (flex column default `align-items:stretch`), so the popup's horizontal-center math used the whole row's width instead of the actual text width and the arrow ended up pointing at empty space — fixed with `align-self:flex-start` on `.nav-dropdown-link`. (2) Per client feedback, moved the popup from below the link to beside it (right side by default, vertically centered, arrow on its left edge), flipping to the left side via `.is-flip` when there isn't enough room before the dropdown's right edge (last column or two) — verified the flip math and centering with direct `getBoundingClientRect()` checks rather than screenshots, since this environment's screenshot tool lagged behind actual DOM state by a step or two when checked between separate tool calls (each individual JS check was internally consistent when run atomically).

**2026-08-14** — Split several combined mega-menu entries into individual services per client feedback: "Identidad de marca & Rebranding" → separate "Identidad de marca" / "Rebranding"; "Diseño y desarrollo web" → "Diseño web" / "Desarrollo web"; "Optimización y automatización" → "Optimización de embudos" / "Automatizaciones"; also added "Consultoría de marca" under Brand Strategy (wasn't listed before). Same `index.html` mega menu from the entry below.

**2026-08-14** — Added a Superside-style mega menu under the desktop nav's "Servicios" link (`.nav-item`/`.nav-dropdown` in `index.html`/`styles.css`), opening on `:hover`/`:focus-within` — no JS, pure CSS. Grouped the existing hub-diagram services (already in the `#servicios` section) into the same 6 capability categories used elsewhere on the site (Brand Strategy, Brand + Creative, Digital Experience, Growth Marketing, Paid Media, Marketing Intelligence) as a 3-column grid, each with a colored dot matching that category's `hub-*` accent color. Desktop-only — `.nav-dropdown { display:none }` below 900px, since mobile nav is the separate burger/`.nav-mobile-links` panel, untouched. **Testing note**: this environment's browser-automation `hover` action doesn't trigger real CSS `:hover` (confirmed against the pre-existing `.nav-links a:hover` color rule too, not just the new code) — verified the dropdown's positioning/content/styling instead via `:focus-within` by calling `.focus()` on the trigger link directly, which is unaffected by that limitation.

**2026-08-14** — Removed a duplicate client-logo marquee entry: "Epikastore" (`index.html`, both copies of the doubled marquee loop) was a logo-only stand-in for the same client already shown as the real "Epika Store" project (hero mosaic + full work-item case study). Found while reviewing the site for duplicated copy — see the new pending item above for 4 other marquee entries (Bary, Bilac, Tiny, Amlogo) that share identical placeholder text and, for 3 of them, point their lightbox image at the wrong project's photo; those weren't touched since there's no real copy/images to replace them with yet.

**2026-08-14** — Synced each Proceso step card's reveal (and its dot lighting up) to the exact moment the rocket arrives at that step's dot, instead of an offset guess. The rocket's angle formula (`angleDeg = -90 + mProg*360`) puts it on dot `i` exactly when `mProg === i/4`, so `stepThreshold` and the dot-lit check in `updateScrollScrubs()` (`index.html`) now both use that same `i/N` fraction — was `(i+0.3)/N` for cards and `(i+0.5)/N` for dots, two different offsets that didn't line up with either the rocket or each other. Verified at `mProg≈0.25`: rocket sits exactly on dot 2 and "02 — CORE" is the only card past card 1 that's visible.

**2026-08-14** — Corrected the above: client actually wanted the rocket to keep flying around the ring with scroll, just always facing the same "sideways" direction instead of rotating to follow the circle's tangent. Restored the JS position trig (`angleDeg`/`rocketX`/`rocketY` → inline `left`/`top`) in `updateScrollScrubs()`, but this time left rotation out of it — `.method-rocket` in `styles.css` now carries a fixed `transform: rotate(-45deg)` that JS never touches, so the glyph's heading stays constant all the way around the loop.

**2026-08-14** — (superseded a few minutes later, see entry above) Parked the Proceso loop's rocket permanently at the top of the ring (12 o'clock, above card 1) instead of flying it clockwise around the circle in sync with scroll.

**2026-08-14** — Fixed the Proceso circular loop's `ms-1` ("01 — INSIGHT") card overlapping the fixed "PROCESO" watermark (`.outline-word`) on wide/short desktop viewports — reported with a screenshot at ~1920px wide. The watermark can't move (absolutely positioned, independent of the loop). Fix: added `margin-top: clamp(24px, 4vw, 80px)` to `.method-loop` at the `min-width:1300px` breakpoint to push the whole ring+cards group down for clearance, and widened `--card-w` from `clamp(220px,14vw,260px)` to `clamp(220px,18vw,340px)` so cards get wider (not taller) on big screens — shorter card height means less upward push needed for `ms-1`, which is anchored above the ring. Verified at 1440px and 1920px: watermark and card no longer intersect.

**2026-08-14** — Replaced the case-study lightbox's dead 4-image grid (referenced `#lightboxImg1`–`4` elements that no longer existed in the markup, so the collage area rendered empty) with a real, generic `ProjectCollage` system — see "Key decisions & conventions" above for how it works. No behavior change for existing projects (still get the seeded-from-pool fallback since none of them set `data-images` yet); this just makes the modal actually show images again and gives future projects a way to opt into a real curated multi-image set without any per-project CSS/layout work.

**2026-08-12** — `e5a97f0` Made the Proceso circle genuinely responsive instead of hand-tuned per breakpoint. Client asked for the cards to scale with viewport width and for the ring to shrink (giving cards more room) after several rounds of manually re-tuning fixed px values (210→270→320px card width, 190→250→300px margin-right, 1360→1500px breakpoint) every time a size changed. Replaced all of it with `--loop-w`/`--card-w`/`--card-push` custom properties on `.method-loop` — the ring width, card width, and the `margin-right` protrusion-buffer are all `calc()`'d off the same three values, so the buffer is *always* mathematically correct for whatever the ring/card sizes resolve to at any viewport, not a fixed guess. Ring shrunk to `clamp(280px,22vw,360px)` (was 420-520), cards grew to `clamp(230px,20vw,320px)` (was a fixed 320px) — verified empirically (no overlap, nothing clips the viewport edge) at 1280/1300/1360/1500/1920px, and the safe circular-mode breakpoint dropped back to 1300px. **Convention**: if this section's sizing needs to change again, adjust the three custom properties in the `@media (min-width:1300px) .method-loop` rule — do not reintroduce fixed px values for card width or margin-right, that's exactly what caused the repeated overlap bugs in every prior round.

**2026-08-12** — `f482821` Client reversed the linear revert ("debe ser circular") — restored the circular Proceso loop, enlarged the cards further, and moved the PROCESO watermark back to its default top-right spot (behind the top card, same layering pattern as elsewhere on the site). Found a real overflow bug while enlarging: bigger cards pushed the right-side card (ms-2) past the viewport edge at desktop widths below ~1360px, since it protrudes ~175px past the ring's own right edge and the ring was pushed flush to the container edge. Fixed with `margin-right:190px` on `.method-loop` plus raising the circular/mobile-fallback breakpoint from 1024px to 1360px. **Convention going forward**: this section has flip-flopped between linear and circular multiple times in one session — before changing it again, check current git blame/this log for the latest state and reasoning rather than assuming either version is "the" design.

**2026-08-12** — `c03508e` Reverted the circular Proceso loop (see `ee2da76` entry below) back to the original linear left-to-right timeline. After several rounds of visual bugs (watermark overlap, loop too close to the text, an apparent duplicate/"ghost" circle) client said "no me gusta, deja proceso donde estaba y mejor haz las cards las largas" — reverted `.method-track`/`.method-timeline`/`.rocket`/`.timeline-dot`/`.method-connector` CSS and the linear scroll-scrub JS from before `ee2da76`, kept the updated Insight/Core/Brand+Experience/Back-to-Insight copy, and made the cards bigger than they originally were (`minmax(230px,1fr)` vs 180px, more padding, `min-height:180px`, larger heading/body text) per the "make cards longer" ask. **Debugging note that ate most of this round**: most of the "my CSS fix isn't showing up" confusion during the circular-loop attempts turned out to be the **browser caching `styles.css`** across reloads/navigations — `document.styleSheets[...].cssRules` would show stale content even after confirming via `fetch()` that the server was serving the updated file, and even after a `force:true` navigate. Swapping the `<link>` element's `href` to `styles.css?v=<timestamp>` via JS forced a real refetch and immediately showed correct rules. If a CSS change ever appears not to apply during local testing, try that cache-bust trick before assuming the code is wrong.

**2026-08-12** — `ee2da76` Turned the Proceso section into an actual circular loop instead of a straight left-to-right timeline, per client request ("podemos hacer que la parte del proceso sea circular?" → explicitly chose the most ambitious of 3 options: keep the scroll-pinned rocket animation but make it fly in a real circular orbit, not just curve the connector lines). The 4 step cards now sit at compass positions around an SVG ring; a lima progress arc fills in with scroll; the rocket's position is computed with trig in JS (`angleDeg = -90 + mProg*360`, `x = 50 + 32*cos`, `y = 50 + 32*sin`) each scroll tick, replacing the old `--prog` CSS-custom-property approach (which only worked for the old linear `left: calc(var(--prog)*100%)` motion — a circle needs both x *and* y computed together, which a single CSS var can't do). Desktop-only (721px+); mobile keeps the pre-existing simple stacked-cards fallback with no circle/rocket, unchanged. **Key lesson for later edits to this section**: cards are positioned relative to the ring's own dot positions (18%/50%/82%, pushed outward by the card's own size + 20px) rather than the container's 0/50/100% edges — anchoring at the container edge put the (much taller) card right on top of the visible ring. If the ring radius (`r=32` in the SVG, `stroke-dasharray`/circumference `201.06`) ever changes, the dot `top`/`left` percentages and the JS `r=32` must be updated together or the rocket/dots/arc will drift out of sync with the visual ring.

**2026-08-11** — `bb1c78d` Major content-only pass (client explicitly said "no forma, solo contenido" — no CSS/structure changes beyond bumping two max-height caps to fit longer text): rewrote Labs section copy (added mission intro, updated Insight/Core/Brand & Experience Lab descriptions + capability pill lists to the client's new "Labs" framing; Academy Lab left untouched — client wants to keep it even though it's not in their new copy), replaced the Servicios right-column pills with the 6 "Capabilities" categories (Brand Strategy, Brand + Creative, Digital Experience, Growth Marketing, Paid Media, Marketing Intelligence — client said capabilities go in Labs descriptions **and** here), rewrote Proceso from a linear 4-step method to the Insight→Core→Brand+Experience→back-to-Insight loop, and rewrote category/description/capabilities for all 22 "Selected Projects" (converted 5 logo-only marquee entries — Clínica del Cerebro, Carlos Ravelo, Caminos de la Vida, La Marquessa, Ser Niños — into full project cards with real lightbox copy). Added a `data-capabilities` attribute to the lightbox JS (falls back to the old comma-split-of-desc behavior if absent) so project tags can be explicit "Brand Strategy · Brand + Creative"-style capability lists instead of parsed from the description text. **Convention**: when adding capability/service lists to `.labs-services` or similar collapsible content, check `max-height` isn't accidentally capping it (this bit us once already on `.work-hidden`, see below) — bumped `.labs-services` to 420px and `.labs-content` (mobile accordion) to 1000px preemptively here.

- **2026-08-11**: Fixed the "open the workshop" door (`.work-door`) so it actually rotates in 3D instead of looking like a flat slide. Root cause: `.work-door-leaf` (the animated element) is a *grandchild* of `.work-door` (the element with `perspective`), with `.work-door-frame` in between — that middle element needs `transform-style: preserve-3d` or the browser flattens the leaf's `rotateY`/`translateZ` into 2D before the ancestor's perspective ever applies. **Key lesson**: any future 3D-transform element (perspective on an outer wrapper, animated transform on an inner element) needs `preserve-3d` on every element in between, not just leaf-level styling — check this first if a "3D" effect looks flat.
  - Hinge is on the **right** edge (`transform-origin: right center`, hinge dot at `right: 3px`), leaf swings open to the left/forward revealing the arch, read by the client as "opens to the right" (the leaf itself ends up positioned toward the right). Open-state transform: `rotateY(58deg)` — **positive** rotateY, not negative. With `transform-origin: right`, negative rotateY sends the free edge away from the viewer (shrinks into an unreadable sliver — tried this first, client said "quedó mal"); positive rotateY brings it toward the viewer instead, reading as a real door swinging out. **Rule of thumb**: the sign of rotateY that "pops toward the viewer" flips depending on which side (`left`/`right`) `transform-origin` is on — always check visually, don't assume the sign from the left-hinge case carries over.
  - No `translateZ` on the leaf's open/hover transform. `transform: rotateY(Xdeg) translateZ(Ypx)` applies right-to-left, so translateZ shifts the *entire* box — pivot included — forward before rotating, which visibly dragged the hinge corner out of the frame instead of keeping it pinned (client caught this: "la esquina derecha no se puede mover"). Plain `rotateY(58deg)` around `transform-origin: right center` keeps the right edge exactly in place and only swings the free edge, which is the correct hinge behavior.

**2026-08-11** — `e1f5e22` Added real photos for 5 brands (AM Studios, Geco., Tata's Photos, Epika Store, Centro del Sueño y Ronquido — converted the first three back from color-cards to real `<button data-lightbox>` items) and refreshed the mockups for the 8 brands that already had photos (same filenames, so every existing reference picked them up automatically — no HTML changes needed for those). Wired the 5 new ones into the hero mosaic, the Trabajo reveal grid, and the lightbox photo pool. Source files: `C:\Users\majot\Desktop\Fotos para sitio\para el hero`, resized to 840x1260 and re-encoded as JPG before adding.

**2026-08-11** — Door open-state iteration (several commits): went back and forth between a flat two-piece void+leaf split (fully legible but user felt it wasn't "3D enough") and a real `rotateY` hinge rotation (reads as physically opened but fights with label legibility the more it rotates). Landed back on the hinge rotation per explicit request ("la de bisagra"), hinge on the **right** edge (`transform-origin: right center`) matching a real door, with the handle dot on the **left** edge of the leaf so it naturally sits at the frame's left edge when closed and right at the void/leaf seam when open — one element instead of two overlapping ones. **Convention**: this door's open-angle is a legibility/dramatic-swing trade-off that's been re-litigated many times — before changing the rotateY angle again, check `.work-door.is-open .work-door-leaf` current value and the rendered leaf width via `getBoundingClientRect()` rather than guessing from a screenshot.

**2026-08-11 (later)** — More door back-and-forth: client pushed the open angle up to 120deg, which passes 90deg and shows the leaf's *back* face (browser mirrors the front content), so added a `scaleX(-1)` counter-mirror on the logo/label for `.is-open`. Client then said the extreme angle looked worse than an earlier, subtler version they'd seen — reverted to a **much smaller open angle (~22–38deg)**, which stays under 90deg, so the `scaleX(-1)` counter-mirror rule was **removed again** (it actively broke the text at low angles, since there's no back-face to counter). Client then asked to flip which side the door opens toward one more time — hinge moved from **right → left → right again** over the session. **Current final state**: hinge on the right (`transform-origin: right center`, hinge dot `right: 1.29cqw`), open transform `rotateY(38deg)`, no `scaleX(-1)` anywhere. **Convention**: if the open angle is ever pushed back above ~85–90deg, the `scaleX(-1)` counter-mirror on `.work-door-mark-ring`/`.work-door-label` needs to be re-added (removed in this round) — check the current `rotateY` value on `.work-door.is-open .work-door-leaf` before assuming either way.
- Also made the whole door **fluid/responsive at every size**, not just at the 720px/901px breakpoints: `.work-door` now uses `container-type: inline-size` + `width: clamp(150px, 40vw, 232px)` + `aspect-ratio: 232/260`, and every inner element (frame, leaf, hinge, handle, mark, label, sparks) is sized in `cqw` (% of the door's own width) instead of fixed px, so the whole thing scales as one proportional unit continuously instead of jumping between fixed sizes. The old fixed-px 720px override for `.work-door`/`.work-door-frame` was removed as redundant.

**2026-08-10** — `3d26685` Fixed the open-door illusion actually being invisible: `translateZ()` combined with a `rotateY()` hinge rotation compounds in 3D space and can re-magnify a foreshortened element back to nearly its original on-screen footprint, masking the rotation almost entirely. Diagnosed by sampling `elementFromPoint` across the door at 10 x-positions instead of trusting screenshots at this small size — found the leaf still covered ~78% of the frame width despite the rotation. Removed `translateZ` from the open-leaf transform. **Convention**: if a hinge/card-flip rotateY effect ever looks "stuck" or unchanged despite the transform clearly being applied (check via computed `transform`/`matrix3d`), verify the actual rendered footprint with `elementFromPoint` sampling before assuming it's a cache issue — don't add translateZ "pop" to a rotateY hinge without re-checking rendered width.

**2026-08-10** — `812ebe9` Rebuilt the Trabajo section's card row as a fixed CSS Grid (`repeat(5, minmax(0,1fr))` at ≥901px) instead of flex-wrap, which had been wrapping to 3-over-2 on desktop. Door is now explicitly the 3rd item: `[1] [2] [door] [3] [4]`, and the revealed "taller" content is a normal-flow 3-column grid below the row (own open/close state), not inside the row itself — opening it just grows page height, the 5-card row never reflows.

**2026-08-10** — `9229d77` Replaced the Trabajo section's infinite auto-scroll marquee with an "Abrir el taller" door-reveal interaction: a small illustrated door (WoW purple + lima, not a generic button) sits after the 4 initially-visible project cards; click swings it open and the rest of the existing projects (no new ones added) rise into view with a staggered animation, click again to reverse and collapse. Also added a `data-match` micro-interaction — hovering a brand logo below highlights its matching project card (only wired for the 3 confirmed logo/photo pairs: Milán, Arlo, Orbit).

**2026-08-10** — `58ae0f1` Labs panels: each lab's name pill now toggles a list of that lab's services (visible by default, click to hide/show), and clicking a service opens the project form. `a672406`/`ce52cd7` also restyled the lightbox: tag pills filled with WoW purple then the bottom caption bar itself changed from near-black to WoW purple (with tag pills switched to translucent white so they still contrast).

**2026-08-09** — `54e2696` Renamed `hero-photos/hero-1..8.jpg` to their real brand names (identified from logos/watermarks visible in the photos) and corrected every title/category/description across the hero background, Trabajo carousel, and marcas marquee that had been using placeholder names (Bary, Bilac, Carlos Ravelo, etc. for the wrong photos). Added 8 brands with no photo yet as colored gradient cards. See file for the full real-brand list the user provided — still pending: real photos for the 8 colorcard brands, and confirming whether `clients/tiny.png` is actually the Tin-T! logo.

**2026-08-09** — `ca5d45e` Fixed the case-study lightbox modal getting an internal scrollbar on short viewports. The 2x2 photo grid used `aspect-ratio: 4/3` per cell, so its height was purely a function of modal width, not available viewport height — switched `.lightbox-inner` to a flex column so the grid flexes to fill exactly the space left after the caption, always fitting within `max-height: 90vh` with no scroll.

**2026-08-09** — `222e360` Fixed falling icons (contact section) not being draggable on any desktop display with OS/HiDPI scaling ≠ 100% (e.g. Windows 125%). Root cause: `Render.setPixelRatio` scales the canvas but Matter's `Mouse` module has its own separate `pixelRatio` (default `1`) used to convert cursor position into world coordinates — left unset, the two drift apart by the scale factor and drag hit-testing silently misses. Now synced on init and on resize.

**2026-08-09** — `242fa5c` Hero photos wired into the existing lightbox (reusing the Trabajo section's brand mapping), and breathing room added between the "LABS" outline word and its panel cards. (A third change — shrinking the "Cómo pensamos" scroll-pin height — was tried, then explicitly reverted per feedback: keep that section's height as original.)

**2026-08-08** — `8d92516` Fix labs accordion breakpoint mismatch (JS gated at 720px, CSS at 900px).

**2026-08-08** — `aed74dd` Labs section: tap-to-expand accordion on mobile/tablet (≤900px), matching Superside reference — collapsed color bars, one open at a time.

**2026-08-08** — `9ef5da8` Mobile/tablet responsive QA pass: fixed undersized footer social tap targets (16px → 40px). Full audit (overlap/overflow/tap-targets) at 320/390/768px came back otherwise clean.

**2026-08-08** — `3b1d295`, `761a723`, `051e954` Rebuilt the mobile/tablet-portrait hero to match the Superside reference: centered text block, photo mosaic as horizontal scroll strip(s) instead of vertical columns. Fixed two "flex-basis becomes height on axis flip" bugs found along the way (hero-content, media-col).

**2026-08-08** — `c79206b`, `6749ab0`, `0566665`, `03d7fb3` Hero desktop positioning iteration: mosaic pinned flush to the right edge via `margin-left: auto` + `flex-grow: 0` on hero-content (final state); tried and reverted a mobile "bleed to edge" approach per user feedback.

**2026-08-08** — `1f74377` Added `--page-x` responsive horizontal padding variable, applied across nav/hero/sections (was a fixed 40px before, felt too tight on wide screens).

**2026-08-08** — `1648840`, `56c33bd` Bigger hero mosaic cards (hero is now taller). Redesigned the project/client click modal into a Superside-style case study layout (2x2 photo grid + dark info bar with name/category/description/tags).

**2026-08-08** — `52158f6`, `d57d360` Slowed the "Cómo pensamos" scroll-pin (more scroll distance before release). Swapped Trabajo/marcas marquee directions (work items left→right, brands right→left, opposite of each other).

**2026-08-08** — `0a86395`, `c03876d`, `a62edf4`, `5141ac2`, `a7ecd80` Falling icons physics tuning: fixed the real bug where an `afterUpdate` handler was killing the bounce every frame; iterated restitution/solver settings back and forth per user preference to land on bouncy + pile-up behavior; hardened drag-release across mouseup/touchend/touchcancel/pointerup/blur.

**2026-08-08** — `2328b18` Added the project form modal (matches a Superside-style "Book a demo" reference), dynamic JS-drawn connector lines in the services hub diagram, real wow-mark icon for the hub "W" (was plain text), and transparent/cropped client logos (flood-fill background removal via Pillow).

**2026-08-08** — `41780d9` Fixed the custom cursor in Servicios rendering behind the pills (a `.services > *:not(.outline-word)` rule had higher specificity than `.figma-cursor` and was overriding its `position:fixed`/z-index).

**2026-08-08** — `e6b1c59` Hero width/flex fixes, Labs/hub-W/contact/marcas visual updates, merged the brands section into Trabajo as a marquee row underneath the project carousel.

**2026-08-08** — `5057b26`, `8ddf134` Hero mosaic now extends behind the (transparent-until-scroll) header with a top/bottom fade mask; fixed the mosaic's infinite-scroll loop jump (`.media-col` was stretching to the container height instead of using its real content height for the `translateY(-50%)` math).

**2026-08-08** — `ae2beec` Large rework: hero, nav, labs, services hub, contact section, new Trabajo/lightbox sections — this is the session where most of the current structure (sections, JS interactions) was established. Removed the old "Esencia" section, redesigned "Cómo pensamos", labs hover panels, services hub diagram, footer socials, contact form CTA.

**2026-08-07** — `e398a52`, `abfa5a5`, `0e435c3`, `bc90315`, `f918c2b` Initial build and early iteration: custom EfectoWow font, interactive services diagram, scroll-synced process timeline, physics-based falling icons (Matter.js), scroll-pinned sections, labs hover panels, hero photo marquee.
